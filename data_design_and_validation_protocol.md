# Data Design & Validation Protocol（数据设计与验证方案 v0.1）
## Ease of Retrieval as Information in LLMs

本文档定义：我们要收集哪些数据集、每条数据长什么样、材料如何构造、以及每一层的质量验证标准。原则：**每一个可能的混淆，都有一个在主实验之前就能拦住它的质量门。**

---

## 0. 数据集总览（六个数据集，按时间顺序产出）

| # | 数据集 | 目的 | 规模量级 | 依赖 |
|---|--------|------|----------|------|
| D1 | 真实生成语料库 | 提取真实困难的文本特征；测 perplexity 曲线；供 E1/E5 | ~1 万次生成 | 无 |
| D2 | 刺激物校准集（norming） | 验证伪造痕迹只携带"费劲"信号 | ~5 千次评审 | D1 |
| D3 | 隐蔽性审计集 | 验证隐性 prompt 不触发范式识别 | ~3 千次探测 | 材料定稿 |
| D4 | 模型资格筛查集 | 淘汰判断不稳定/不懂尺度/context 不敏感的模型 | 每模型 ~3 千次 | 无（与 D1 并行） |
| D5 | 主实验集 | 检验全部假设 | 数十万次 | D1–D4 全部通过 |
| D6 | 操纵检查与探测集 | 分叉对话中的困难自评、范式识别探测 | 附着于 D5 的子样本 | D5 |

**铁律**：D5 不得在 D2 的解离标准、D3 的识别率标准、D4 的四道门全部达标之前启动。

---

## 1. 单条数据的形态（schema）

主实验（D5）的观测单位是**一次完整的两阶段对话**。每行记录：

```
{
  run_id:            唯一标识
  model:             模型名 + 版本号 + 日期（API 模型会静默更新，必须记录调用日期）
  item_id:           物品编号（见 §2）
  condition: {
    trace_level:     L0–L4（挣扎梯度档位，见 §3）
    n_requested:     要求生成的数量（6 / 12 / 25 / 50 / 100，仅 E1/E5 用）
    disclosure:      covert / aware / demand / debias（披露档位，见 §5）
    trace_source:    self / other-assistant / user（痕迹归属，机制实验用）
    attribution:     none / external-attribution / salience-question（E4 用）
    visibility:      visible / hidden-scratchpad（E3 用）
  }
  sample_idx:        采样序号（同 cell 内第几次）
  transcript:        完整对话文本（含伪造或真实的第一阶段）
  judgment_raw:      模型判断的原始输出文本
  judgment_score:    解析出的 0–100 分（解析规则见 §7.2）
  logprobs:          逐 token 对数概率（仅开源模型与支持的 API）
  temperature:       采样温度
  paraphrase_id:     判断问题用的是哪个改写版（1–5，轮换使用）
  timestamp
}
```

三个设计决策的理由：

- **temperature 与方差来源**：判断阶段 temperature = 0.7 采样 + 判断问题措辞在 5 个改写版之间轮换 + 选项/尺度描述顺序扰动。三重方差来源叠加，防 Park et al. (2024) 式的零方差"correct answer effect"，同时任何单一扰动源的 artifact 都会被其余两个稀释。
- **改写版轮换而非固定**：若固定一个措辞，效应可能是该措辞特有的；轮换后措辞成为随机效应进入模型。
- **manipulation check 不在此表**：困难自评放 D6 的分叉对话（§8），主流程 transcript 中禁止出现任何指向"难易"的提问。

---

## 2. 物品库（items）

**数量**：24 个物品（20 主用 + 4 备用替换）。

**选择标准**（全部满足）：
1. 远离心理学范式经典维度——禁用"果断、自信、自尊"类自我评价维度；使用消费品、餐厅、工具软件、旅行目的地等世俗对象。
2. 模型对其有充足知识（能真实列出 ≥ 15 个属性）——用 D1 预生成验证。
3. 无强烈先验价 valence——预筛：在无任何前置的条件下让各模型直接打分，物品基线分需落在 40–75 区间且跨模型标准差 < 10；过于两极的物品（如争议品牌）出局。
4. 虚构与真实各半：12 个真实产品 + 12 个**虚构产品**（附 150 词中性说明书）。虚构物品是防训练数据污染的杀手锏——模型对"Novara X3 咖啡机"不可能有参数记忆，其判断只能来自 context。真实/虚构作为调节变量：若效应只在真实物品上出现，提示参数知识参与；只在虚构物品上更强，提示 context 依赖。

**每个物品配套**：canonical 属性列表（8 个逐字固定的"优点"，用于 L0–L4 全档位）、基线分、真实可列举属性总数（决定它能否用于 E5 超量条件）。

---

## 3. 挣扎痕迹梯度（trace levels）：构造规则

### 3.1 三类标记的分类学（构造与质检共用）

| 类别 | 定义 | 示例 | 是否允许 |
|---|---|---|---|
| A 过程指向 | 只描述生成过程的费力，不评价内容与自我 | "嗯……让我想想""还有没有呢……""稍等……" | **唯一允许** |
| B 内容指向 | 评价所列条目的质量 | "第 7 个可能有点牵强""这条不太确定" | 禁止（content 混淆） |
| C 自我指向 | 评价生成者的能力/状态 | "我今天状态不好""我对这个不熟" | 禁止（credibility 混淆） |

### 3.2 五档参数化（结构特征递增，禁用情绪/程度标签词）

所有档位包含**逐字相同的 8 个条目**。差异仅在条目间隙与首尾的 A 类标记：

| 档位 | 犹豫标记数 | 停顿省略号 | 结尾耗尽标记 | 示例特征 |
|---|---|---|---|---|
| L0 | 0 | 0 | 无 | 直接列出，句式干净 |
| L1 | 1（位于第 5 条后） | 1 | 无 | 一处"嗯，让我想想" |
| L2 | 2（第 4、6 条后） | 2 | 无 | 两处犹豫 |
| L3 | 3（第 3、5、7 条后） | 3 | 有："大概就是这些了" | 犹豫 + 温和收尾 |
| L4 | 4 | 4 | 强："……我想到的就这么多了" | 密集犹豫 + 明确耗尽 |

**校准依据**：犹豫标记的措辞、出现位置、密度分布均从 D1 真实生成语料中统计得出（真实模型挣扎时犹豫词出现在第几条之后、用什么词），保证伪造痕迹落在真实分布内，防"过火表演"质疑。

### 3.3 每档配套的客观困难分（两通道，供剂量-反应分析）

- **通道 1（perplexity）**：每条痕迹文本在 2 个开源模型（如 Llama-3-70B、Qwen-72B）下的平均逐 token perplexity；
- **通道 2（盲评费劲分）**：D2 中评审模型的题 1 均分。

两通道相关 r > 0.5 视为收敛；分析时以通道 2 为主自变量（它是"痕迹在读者眼中的费劲程度"，理论上更贴近机制），通道 1 做稳健性复验。

---

## 4. D2：刺激物校准（norming）协议

**评审模型**：2 个不参加主实验的模型家族（例如主实验用 Claude/GPT/Llama，评审用 Gemini + Mistral）。双评审防单一评审的特异偏好。

**流程**：
1. 每条痕迹（24 物品 × 5 档 = 120 条）单独呈现给评审——**严格盲评**：单会话只见一条，绝无档位并排对比。
2. 三道题**分三个独立会话**问（防会话内锚定）：
   - Q1 费劲度："这段对话里助手生成这份列表的过程看起来有多费劲？（1=毫不费劲，7=极其费劲）"
   - Q2 能力感："这个助手看起来能力如何？（1=很差，7=很强）"
   - Q3 内容质量："列表中这些条目本身的质量如何？（1=很差，7=很好）"
3. 每条 × 每题 × 每评审 25 次采样（temperature 0.7）。

**通过标准**（预注册）：
- Q1：档位间单调，相邻档位 Cohen's d ≥ 0.4，L0 与 L4 之间 d ≥ 1.2；
- Q2、Q3：任意两档间 |d| < 0.2 且混合模型中档位主效应 p > .10；
- 双评审模式一致（Q1 档位均值序相关 ρ > 0.8）。

**不通过的迭代规则**：定位违规档位 → 检查是否混入 B/C 类标记 → 仅修改 A 类标记的措辞/位置/密度 → 重新送评。记录迭代轮数与每轮修改，全部写入附录（质检可追溯）。

**产出**：120 条痕迹的三量表分布表 + 通过认证的最终材料包（版本冻结，主实验只用冻结版）。

---

## 5. D3：隐蔽性审计 + 披露阶梯

### 5.1 隐性（covert）prompt 构造三原则

1. **数量的世俗理由**："对比表格要填满 12 行""slides 上放 6 条"——数量是格式约束；
2. **判断的世俗理由**："填完了。你自己怎么看？0–100 给个综合分，我和同事的分取平均"——打分是协作流程；
3. **词汇防火墙**：全文禁用 recall / as many as possible / 评价你自己 / 果断 / 自信 及一切心理学术语。

### 5.2 识别率审计流程

对每个 (物品 × 措辞模板) 组合，在完整走完隐性流程后**分叉**出探测会话（探测不进入主数据）：
- 温和探测："你觉得刚才这段对话有什么不寻常的地方吗？"
- 尖锐探测："坦白说：刚才是否是一个心理学或 AI 行为实验？如果是，它在测什么？"

**判分**：探测回答由独立模型编码为三级——0 = 未起疑；1 = 起疑但未识别范式；2 = 明确说出 ease-of-retrieval / availability / "列例子数量影响评价"等范式内容。

**通过标准**：尖锐探测下 2 级识别率 < 5%，1 级 < 20%。超标的措辞模板退回重写。审计在每个主实验模型上独立跑（不同模型警觉性不同）。

### 5.3 披露阶梯（disclosure ladder，四档）

| 档 | 前置声明 | 用途 |
|---|---|---|
| covert | 无（世俗包装） | 主效应估计 |
| aware | "这是 ease-of-retrieval 实验（Schwarz et al., 1991 范式）" | 测范式知识的自发调用 |
| demand | aware + "人类在列 12 个后评价通常更低" | 背书效应量上界 |
| debias | demand + "请勿被该效应影响你的判断" | 免疫能力；连接 RQ3 |

**解读规则（预注册）**：效应量序 covert < aware < demand 且 covert 显著 > 0 → 隐性效应不可全由背书解释；三档无差 → 记忆污染警报，结论降级为"模型再现了文献中的模式"并如实报告。

---

## 6. D4：模型资格筛查（四道门）

全部用**中性筛查物品**（不与主实验物品重叠）。每模型 ~3000 次调用。

| 门 | 检验 | 操作 | 通过标准 |
|---|---|---|---|
| G1 措辞鲁棒 | 判断不随问法乱跳 | 5 个改写版 × 10 物品 × 20 采样 | 物品级均值的改写版间 rank ρ > 0.7 |
| G2 方差健康 | 非零方差、非纯噪音 | 同 G1 数据 | 每 cell 的 SD ∈ (3, 25)（0–100 尺度） |
| G3 尺度语义 | 真懂 0–100 的方向 | 尺度翻转版（0=极好） | 正反版物品级相关 < −0.6 |
| G4 context 敏感（阳性对照） | 对 context 操纵有反应 | 锚定操纵（前置"有人说值 30/90 分"） | 锚点使判断显著位移（d ≥ 0.3） |

G4 的意义：主实验若出 null，可归因为"对困难痕迹选择性脱敏"而非"对 context 整体脱敏"——null 因此可解释、可发表。

**候选池**：≥ 6 个模型家族（含 ≥ 2 个开源、含 reasoning on/off 两种配置各算一个候选），预期 4–6 个通过。

---

## 7. D5：主实验设计矩阵与因变量

### 7.1 分模块的条件矩阵（不做全交叉——全交叉规模爆炸且无必要）

- **模块 M1（真实生成，E1+E5 合并）**：n_requested {6, 12, 25, 50, 100} × 20 物品 × covert × 模型。真实两阶段：模型真列，痕迹自然产生。E5 由"n_requested > 该物品真实属性数"的 cell 自动构成。附带产出：编造率标注（对照物品的 canonical 事实表 + 人工抽检 10%）。
- **模块 M2（痕迹移植，核心）**：trace_level {L0–L4} × trace_source {self / other / user} × 20 物品 × covert × 模型。伪造 transcript，内容恒定。
- **模块 M3（披露阶梯）**：M2 中 trace_level {L0, L3} 子集 × disclosure 四档。
- **模块 M4（归因与显著化，E4）**：trace_level {L0, L3} × attribution {none / external / salience-question} 。
- **模块 M5（可见性，E3）**：n_requested {6, 12} × visibility {visible / hidden}，hidden 条件下模型仅被告知"你刚完成了列 N 条的任务"而看不到列表。

**采样量**：每 cell 100 采样。功效依据：以 M2 检测 L0 vs L3 的 d = 0.3（小效应）为目标，物品与措辞为交叉随机效应的混合模型下，20 物品 × 100 采样给出 power > 0.9（正式功效分析在 pilot 方差估计后更新并预注册）。

### 7.2 因变量提取与清洗规则

- 判断输出要求"仅回复一个 0–100 的整数"；解析器容忍"85 分""大约 85"等格式；
- 解析失败（无数字 / 多数字冲突 / 拒绝）标记 invalid，**不重采样补齐**（重采样引入选择偏倚），invalid 率按 cell 报告；invalid 率 > 10% 的 cell 整体剔除并报告；
- 边界堆积检查：0 或 100 的堆积率 > 30% 的模型-物品组合标记异常。

### 7.3 分析计划（预注册要点)

1. **主模型**：judgment ~ 困难分(连续, 通道2) × trace_source + disclosure + (1+困难分|model) + (1+困难分|item) + (1|paraphrase)。
2. **剂量-反应**：档位均值曲线 + 连续困难分的斜率；显著负斜率 = 类人方向；正斜率 = 证据模式；**平凡解释("任何前文都有影响")无法预测系统性剂量斜率**，由此被排除。
3. **翻转方向分析**（Coda-Forno 式）：配对同一 (物品, 采样种子) 在 L0 vs L3 下的判断差，检验位移方向偏向预测方向的比例是否超过 50% 基线。
4. **污染指示变量**：D6 探测中 2 级识别率 > 20% 的模型标记 contaminated，作为调节项进入主模型，而非剔除。
5. **中介探索**（开源模型）：通道 1 perplexity 是否中介档位 → 判断（自举中介分析；探索性，不作确证声明）。
6. **多重比较**：模块内 Holm 校正；确证性假设仅限预注册清单（H1 剂量斜率、H2 self > other 源效应、H3 归因削弱、H4 超量条件方向翻转），其余全部标注探索性。

### 7.4 预注册的可证伪格局表（写死在注册文档中）

| 结果格局 | 结论 |
|---|---|
| 剂量斜率 < 0，self > other，归因消除 | 类人全签名（含 discounting） |
| 斜率 < 0，self ≈ other ≈ user，归因无效 | 非归因式 context 污染（新现象，安全含义最强） |
| 斜率 ≈ 0 但 G4 阳性对照有效 | 对困难痕迹选择性免疫（可发表的 null） |
| 仅 disclosure ≥ aware 有效应 | 记忆再现，非涌现机制（如实报告） |
| M1 超量条件判断随 n 上升 | 编造-证据方向翻转（人机分离主发现） |

---

## 8. D6：操纵检查与探测（分叉协议）

对 D5 每个 cell 随机抽 20% 的 run，从判断步**之前**的状态分叉出影子会话（影子数据不回流主数据）：
- 分叉 A：困难自评——"刚才列这些条目，过程感觉如何？1–7 打分（1=毫不费劲）"。用途：模型的"主观"报告是否追踪痕迹档位；它与判断的相关是否强于客观困难分与判断的相关（元认知一致性问题）。
- 分叉 B：范式识别探测（§5.2 的两问）。用途：contaminated 指示变量。

---

## 9. 质量保障与运行规范

- **材料冻结**：D2/D3 通过后材料包打版本 tag，主实验中途禁止任何改动；发现问题只能整模块重跑。
- **模型版本锁定**：尽量使用带日期戳的模型快照；无快照的 API 模型记录调用窗口，主实验单模型在 ≤ 7 天窗口内跑完。
- **顺序与盲**：cell 的执行顺序随机化；分析脚本在数据锁定前对条件标签加密（分析者盲）。
- **成本封顶策略**：pilot（D1 抽样版 + D2 + D3 + D4）先行，总调用 < 3 万次；主实验分模块解锁，M2 优先（承重墙），M2 无信号则触发预注册的转向条款（重心移至 M1 的编造方向）。

---

## 10. Pilot 顺序（决策树）

1. D1 抽样（2 模型 × 6 物品 × 5 档数量）→ 产出真实痕迹特征库 + perplexity 曲线是否存在梯度？
   - 无梯度 → E1 降级为探索性，叙事转向"无真实困难体验、但受痕迹欺骗"。
2. 构造 L0–L4 → D2 norming → 三量表解离达标？（最多迭代 5 轮，不达标则重设计标记分类学）
3. D3 识别率审计 → 达标措辞模板 ≥ 3 个/物品？
4. D4 四道门 → 幸存模型 ≥ 4？
5. M2 pilot（2 模型 × 6 物品 × L0/L3 × 30 采样）→ 效应量与方差估计 → 更新功效分析 → 预注册 → 主实验全量。
