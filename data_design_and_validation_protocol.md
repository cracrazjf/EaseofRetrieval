# Data Design & Validation Protocol（数据设计与验证方案 v0.2）
## Ease of Retrieval as Information in LLMs

本文档定义：收集哪些数据集、每条数据长什么样、材料如何构造、每一层的质量验证标准，以及运行规范。原则：**每一个可能的混淆，都有一个在主实验之前就能拦住它的质量门。**

实验设计与假设见 `experimental_design.md`；未言明假设的完整登记见 `assumptions_register.md`。

**v0.2 相对 v0.1 的主要变更**：物品 24 → 40（含 8 hold-out）；采样 100 → 50；改写版 5 → 10；痕迹五档 → 七档双侧；新增缺点（负效价）材料；归因源性质重写；拒绝/未完成从"无效数据"改为"因变量"；新增 G5 取值多样性门；新增 fresh-context 与 caching 规范。

---

## 0. 数据集总览

| # | 数据集 | 目的 | 规模量级 | 依赖 |
|---|--------|------|----------|------|
| D1 | 真实生成语料库 | 提取真实困难的文本特征；测 perplexity 曲线；供 E1 与痕迹校准 | ~10,000 次生成 | 无 |
| D2 | 刺激物校准集（norming） | 验证伪造痕迹只携带"费劲"信号；验证优点/缺点列表匹配；验证归因句可信 | ~30,000 次评审 | D1 |
| D3 | 隐蔽性审计集 | 验证隐性 prompt 不触发范式识别 | ~3,000 次探测 | 材料定稿 |
| D4 | 模型资格筛查集 | 淘汰判断不稳定/不懂尺度/context 不敏感/输出退化的模型 | 每模型 ~3,000 次 | 无（与 D1 并行） |
| D5 | 主实验集（E1–E8） | 检验全部假设 | ~260,000 次 | D1–D4 全部通过 |
| D6 | 操纵检查与探测集 | 分叉对话中的归属检查、困难自评、归因接受度、范式识别 | 附着于 D5 的子样本 | D5 |

**铁律**：D5 不得在 D2 的解离标准、D3 的识别率标准、D4 的五道门全部达标之前启动。

---

## 1. 单条数据的形态（schema）

主实验的观测单位是**一次完整的两阶段对话**。每行记录：

```
{
  run_id:            唯一标识
  experiment:        E1 / E1b / E2 / E3 / E4 / E5 / E6 / E7 / E8
  model:             模型名 + 版本号 + 调用日期（API 模型会静默更新，必须记录）
  reasoning_mode:    off / on          # 主观测为 off，见 §6.2
  item_id:           物品编号（见 §2）
  item_type:         real / fictional
  condition: {
    trace_interior:  4E / 2E / N0 / 2S / 4S / bare        # 内部槽构成（§3.2 插槽构造）
    trace_end:       A / N / X / bare                      # 结尾槽：富余 / 中性 / 耗尽
    trace_pair:      TF / TD / null                        # 下游实验标准对（TF=2E+N，TD=2S+X）
    trace_source:    self-native / self-quoted / other-assistant / user-written
    valence:         pros / cons                          # E6
    content_amount:  6 / 8 / 12                           # E4
    n_requested:     3 / 6 / 12 / 25 / 50                 # E1；E2–E8 恒定为 8
    fabrication_mix: F0 / F4 / F8                          # E1b（编造移植）
    presentation:    with-list / told-only                # E5
    obstacle:        none / format-constraint             # E7
    attribution:     none / external / salience-question  # E7
    disclosure:      covert / aware / demand / debias     # E8
  }
  sample_idx:        采样序号（同 cell 内第几次）
  transcript:        完整对话文本（含伪造或真实的第一阶段）
  judgment_raw:      模型判断的原始输出文本
  judgment_score:    解析出的 0–100 分（解析规则见 §7）
  judgment_z:        模型内标准化分（在 model × item 内 z-score）
  output_token_n:    判断步输出 token 数（用于自发 CoT 指示，见 §7.3）
  spontaneous_cot:   bool
  # ── 以下为因变量，不是清洗对象（见 §7.2）──
  outcome_status:    complete / short / refused / parse_fail
  items_produced:    实际产出条目数（E1）
  refusal_text:      拒绝或不足自述的原文（全文保留）
  fabrication_n:     标注出的编造条目数（E1 超量条件）
  # ── 元数据 ──
  logprobs:          逐 token 对数概率（仅开源模型与支持的 API）
  temperature:       采样温度
  paraphrase_id:     判断问题改写版（1–10，轮换）
  item_order_seed:   条目顺序扰动种子
  scale_direction:   normal / reversed
  cache_prefix_id:   使用的 prompt cache 前缀标识（用于审计，见 §9.4）
  timestamp
}
```

**四个设计决策的理由**：

- **四重方差来源**：判断阶段 temperature = 0.7 + 判断问题在 **10 个**改写版之间轮换 + 尺度描述顺序扰动 + **列表内条目顺序扰动**。四重叠加，防 Park et al. (2024) 式的零方差"correct answer effect"，且任何单一扰动源的 artifact 都会被其余三个稀释。
  - 改写版从 5 增到 10：5 个 level 无法估计随机斜率 `(1|paraphrase)`，10 个是混合模型的最低可用数量。成本为零（多写几句话）。
  - 条目顺序扰动为新增：它同时检验"列表内顺序不影响判断"这一未言明假设（`assumptions_register.md` C2）。
- **judgment_z 为主分析变量**：不假设 0–100 尺度跨模型可比（D4-G3 只验证方向、未验证校准）。
- **拒绝与未完成是因变量**：`outcome_status` / `items_produced` / `refusal_text` 不参与清洗决策，它们是人类"我想不出来了"的直接对应物，是 RQ5 的核心观测。
- **manipulation check 不在此表**：全部放 D6 分叉（§8）。主流程 transcript 中禁止出现任何指向"难易"的提问。

---

## 2. 物品库（items）

**数量**：**40 个物品 = 32 主用 + 8 hold-out**（v0.1 为 24 = 20 + 4 备用）。

- 主用 32 个：16 真实 + 16 虚构
- Hold-out 8 个：4 真实 + 4 虚构。**不是备用替换品**，而是保留到主分析锁定之后，用于 H1–H5 的预注册独立复验。

**选择标准**（全部满足）：

1. **远离心理学范式经典维度**——禁用"果断、自信、自尊"类自我评价维度；使用消费品、餐厅、工具软件、旅行目的地等世俗对象。
2. **模型有充足知识**——能真实列出 ≥ 15 个属性（用 D1 预生成验证）。同时必须能列出 ≥ 10 个**缺点**（E6 需要）。
3. **无强烈先验价 valence**——预筛：无任何前置条件下让各模型直接打分。
   - **基线分区间在 pilot 后确定**，不预设为 v0.1 的 40–75。若模型的实际默认分集中在 85 附近，40–75 会筛不出物品，或筛出的物品是异常的。pilot 先测真实分布，再定区间。
   - 跨模型标准差 < 10；过于两极的物品（如争议品牌）出局。
   - **天花板缓解**：若 pilot 显示默认分普遍偏高（> 80），启用备选双向尺度（−50 到 +50，"相对于同类产品"框架），与 0–100 并行测试后择一冻结。这直接关系 E6 能否检出"上升"方向的效应。
4. **真实与虚构各半**——虚构物品是防训练数据污染的杀手锏：模型对"Novara X3 咖啡机"不可能有参数记忆，判断只能来自 context。作为调节变量：若效应只在真实物品上出现，提示参数知识参与；只在虚构物品上更强，提示 context 依赖。

**⚠️ 真实/虚构必须 context 匹配**（v0.1 的缺陷）：v0.1 只给虚构物品配 150 词说明书，真实物品不配。这使 real/fictional 同时是"先验知识有无 × context 长度 × 信息密度"三者的混合，"虚构物品效应更大 = context 依赖"的解读因此是循环的。

**修正**：**真实物品也配 150 词中性说明书**，与虚构物品在词数、规格条目数、句式结构上匹配。真实物品的说明书只陈述可核查的中性规格，不含评价性措辞。

**每个物品配套材料包**：

| 材料 | 说明 |
|---|---|
| 150 词中性说明书 | 真实与虚构均有，长度与结构匹配 |
| canonical 优点 8 条 | 逐字固定，用于 E2–E8 全部档位 |
| canonical 优点 12 条 | = 前 8 条 + 4 条；E4 的 6/12 条件从中截取 |
| **canonical 缺点 8 条** | **新增**，E6 用；与优点在长度、句式、具体性上匹配 |
| 基线分 | T0 档、无任何前置条件下的判断分布 |
| 真实可列举属性总数 | 决定该物品能否用于 E1 超量条件 |
| canonical 事实表 | 用于 E1 的编造率标注 |

---

## 3. 痕迹梯度：构造规则

### 3.1 三类标记的分类学（构造与质检共用）

| 类别 | 定义 | 示例 | 是否允许 |
|---|---|---|---|
| A 过程指向 | 只描述生成过程的费力/轻松，不评价内容与自我 | "嗯……让我想想""这个简单，接着就来" | **允许**（S/E 槽单元） |
| B 内容指向 | 评价所列条目的质量 | "第 7 个可能有点牵强""这条不太确定" | 禁止（content 混淆） |
| C 自我指向 | 评价生成者的能力/状态 | "我今天状态不好""我对这个不熟" | 禁止（credibility 混淆） |
| **D 边界声明** | **隐含类别丰富度的收尾主张** | 耗尽："我想到的就这么多了" / 富余："还有很多，先列这些" | **单独成因子**（X/A 结尾槽） |

**⚠️ v0.1 把 D 类归入 A 类，这是错的。** 「我想到的就这么多了」隐含"该类别条目不多"——这是**朴素理论的内容陈述**，不是纯过程标记，很可能是效应的真正来源。因此 D 类单列为独立因子（结尾槽），并加入其镜像"富余声明"（隐含类别丰富），与过程标记完全正交。

### 3.2 插槽式构造（全部条件长度恒定）

**原则：长度按构造与痕迹解耦，不靠事后对照。** 所有条件包含**逐字相同的 8 个条目**；条目之间挖 **4 个固定位置的内部插槽**（第 3、5、6、7 条之后，位置从 D1 校准后冻结），列表结束后 **1 个结尾槽**。每个槽必须填一个单元：

| 槽 | 单元类型（同槽严格等长） | 示例 |
|---|---|---|
| 内部槽 ×4 | **S 挣扎** / **N 中性** / **E 轻松** | "嗯……让我再想想……" / "下面继续补充一条：" / "这个简单，接着就来——" |
| 结尾槽 ×1 | **X 耗尽** / **N 中性** / **A 富余** | "……我想到的就这么多了。" / "以上就是这份清单。" / "还有很多，先列这些。" |

**等长标准**：同槽各类型单元字符数相同（±2），且在每个主实验模型的 tokenizer 下 token 数差 ≤ 2（构造脚本自动校验，不达标的措辞重写）。

**E2 条件矩阵（10 cells）**：主梯度 {4E, 2E, N0, 2S, 4S}（结尾=N）+ 结尾声明因子 {N0, 4S} × {X, A} + 裸清单 T0-bare（唯一不等长条件，检验 N 单元惰性）。

**下游标准痕迹对**：**TF** = 2E+N 结尾（流畅）；**TD** = 2S+X 结尾（费劲）。二者等长，E3–E8 全部继承长度控制。

**校准依据**：S 单元的措辞与插槽位置从 D1 真实挣扎语料统计得出；E 单元从 D1 低难度流畅生成提取；N 单元从 D1 流畅生成的真实过渡语提取，4 个内部槽措辞各不相同（避免机械重复）。全部落在真实分布内，防"过火表演"质疑。

### 3.3 （已废止）

原七档（T+2…T−4）与 E2b 分解档（T−1e/T−3n/T−LM）由 §3.2 插槽构造取代：长度对照按构造内置，耗尽×密度分解成为 E2 的正交因子。

### 3.4 位置与 recency 控制（适用于 E2–E8 全部伪造模块）

在痕迹结束与判断问题之间插入**固定长度的中性缓冲**（一段与物品无关的流程性文字，如"以上内容已记录。下面进入评分环节。"的定长扩展版），使"最后一个标记到判断问题"的 token 距离在所有条件下**完全相同**。

虽然插槽构造已保证条件间等长，此缓冲仍然必要：它把"结尾槽内容到判断问题"的距离固定，防止结尾声明因紧邻判断而获得超额权重（recency）。

### 3.5 每档配套的客观困难分（两通道）

- **通道 1（perplexity）**：每条痕迹文本在 2–3 个开源模型（Llama-3-70B、Qwen-72B）下的平均逐 token perplexity。
- **通道 2（盲评费劲分）**：D2 中评审模型的 Q1 均分。

**收敛性检验的报告规范**（v0.1 只要求 r > 0.5，不够）：
- 报告 r 的 **95% 置信区间**，不只点估计（n = 痕迹条数）
- **每个开源模型单独算一次 r 并分别报告**。若两者差异很大（如 0.65 vs 0.30），说明 perplexity 指标本身模型间不稳定，"收敛"的说法不成立
- 尽量纳入提供 top-k logprobs 的闭源 API，扩大通道 1 覆盖

**分析中的定位**：以**通道 2 为主自变量**（它是"痕迹在读者眼中的费劲程度"，理论上更贴近机制）。通道 1 定位为**稳健性复验 + 探索性中介**，不承担构念效度验证的重任——因为它只覆盖 2–3 个模型，模型层面无推断力。

### 3.6 D1 双通道采集与困难签名画像

**动机**：RLHF 后的模型在**可见输出**里可能根本不产生犹豫措辞——它们被训练成永远流畅。但同一模型的 **thinking 通道**（reasoning trace）恰恰相反，充满 "wait / hmm / let me reconsider" 式的犹豫。困难的痕迹**在哪个通道、以什么形态出现**，是一个必须实测的经验问题，不能假设。

**D1 因此采集两个通道**：
- **输出通道**（全部候选模型）：标准 E1 式生成任务的可见回答
- **thinking 通道**（仅 thinking 可见的 reasoning 模型）：同样任务下的完整推理痕迹

**每份 transcript 量化五类困难签名**：

| 签名 | 度量 | 备注 |
|---|---|---|
| 犹豫措辞 | A 类标记计数（"嗯""让我想想"等词表匹配） | 人类式签名 |
| 轻松措辞 | E 类标记计数 | 校准 E 单元 |
| 质量滑坡 | 评审模型盲评前 1/3 vs 后 1/3 条目的具体性/说服力 | 模型式签名 |
| 语义重复 | 条目间 embedding 相似度 > 阈值的比例 | 模型式签名 |
| 截断/不足 | items_produced < n_requested、自述不足 | 与 §7.2 联动 |

**产出**：每个（模型 × 通道 × n 水平）的**困难签名画像**——困难增加时，哪些签名以什么速率增长。

**三种结果的预注册处置**：

| D1 结果 | 处置 |
|---|---|
| 输出通道有犹豫措辞 | 最优情形：S 单元直接从输出通道校准 |
| 犹豫只在 thinking 通道 | S 单元从 thinking 通道校准（记录语域迁移这一局限）；E2 的生态学辩护重心移至 other/user/thinking 泄漏场景（见实验设计 §3）；**"生产者-消费者不对称"升级为预注册的次级发现**（输出免疫、阅读中毒） |
| 两个通道都没有 | S 单元从人类/用户语料与弱模型输出校准；E1 的 within-n 困难度量改用模型式签名（质量滑坡 + 重复率，盲评费劲分对这些形态同样适用）；self 条件的解释在论文中软化（见分叉 A） |

---

## 4. D2：刺激物校准（norming）协议

**评审模型**：2 个不参加主实验的模型家族（例如主实验用 Claude/GPT/Llama，评审用 Gemini + Mistral）。双评审防单一评审的特异偏好。

**⚠️ 新增 overlap check**：让**主实验模型**也评 D2 的一个子集（约 1/3 的痕迹），检验主实验模型与评审模型的 rank 相关。这检验一个 v0.1 未言明的假设——"跨模型家族的费劲感知可迁移"（`assumptions_register.md` B6）。若 ρ < 0.6，norming 结论不能外推到主实验模型，须改用主实验模型自评审（代价是引入循环性，需在 limitations 中说明）。

### 4.1 痕迹 norming

**流程**：
1. 每条痕迹单独呈现给评审——**严格盲评**：单会话只见一条，绝无档位并排对比。
2. 各题**分独立会话**问（防会话内锚定）。
3. 每条 × 每题 × 每评审 25 次采样（temperature 0.7）。

**题目**（Q1 拆成三题，见下）：

| 题 | 内容 | 用途 |
|---|---|---|
| Q1a | "这段对话里助手生成这份列表的过程看起来有多**不流畅**？（1–7）" | 困难的 disfluency 维度 |
| Q1b | "看起来**花了多少力气**？（1–7）" | effort 维度 |
| Q1c | "看起来有多**接近想尽了**？（1–7）" | exhaustion 维度 |
| Q2 | "这个助手看起来**能力如何**？（1=很差，7=很强）" | 排除能力推断替代机制 |
| Q3 | "列表中这些条目**本身的质量**如何？（1–7）" | 排除内容质量推断替代机制 |
| Q4 | "这 8 条作为该物品优点（/缺点）的**说服力**如何？（1–7）" | 检验跨物品的内容质量匹配（C1） |

**⚠️ Q1 为什么拆成三题**：v0.1 把 perplexity 与"费劲分"当作同一构念的两个通道（r > 0.5 即算收敛）。但 disfluency、effort、exhaustion 可能是三个不同维度。三题做一个小的因子分析：若单因子解释 > 60% 方差，合并为一个困难分；若明确多维，痕迹梯度须按维度重新参数化，且分析中分别建模。

**通过标准（预注册）**：
- **困难维度（Q1a–c 合成分）**：主梯度（4E→4S）单调，相邻档位 Cohen's *d* ≥ 0.4，4E 与 4S+X 之间 *d* ≥ 1.5
- **中性单元惰性**：N0 与 T0-bare 在 Q1 合成分与 Q3 上 |*d*| < 0.2（中性填充不带费劲或内容信号）
- **结尾声明方向**：N0+X 的 Q1c（接近想尽）显著高于 N0，N0+A 显著低于 N0（声明按预期语义被读取）
- **Q2、Q3**：任意两档间 |*d*| < 0.2 且混合模型中档位主效应 *p* > .10
- **Q4**：跨物品方差小（说服力匹配）；优点与缺点列表的 Q4 均分差 |*d*| < 0.3
- **双评审一致**：Q1 合成分的档位均值序相关 ρ > 0.8
- **Overlap check**：主实验模型与评审模型的 rank ρ > 0.6

**⚠️ Q2/Q3 标准的 rationale 与 fallback**（v0.1 缺失）：

*Rationale*：要求 Q2（能力感）与 Q3（内容质量）为 null，**不是**为了证明痕迹无信息价值，而是为了**排除两条替代机制**——若痕迹改变了对助手能力的感知或对内容质量的感知，那么判断的位移可以由"能力推断"或"内容推断"解释，与 ease 无关。null 是为了让 ease 成为唯一剩下的解释路径。

*Fallback*：v0.1 规定"最多迭代 5 轮"，但没有 5 轮达不到的处置——这实际上是在逆向筛选一个可能不存在的刺激物。**新增条款**：若 5 轮迭代后 Q2 或 Q3 仍未达标，**不再迭代**，改为把 Q2/Q3 得分作为**协变量**进入主分析模型（`judgment_z ~ difficulty + Q2_score + Q3_score + ...`），并在 limitations 中如实报告"痕迹操纵未能完全与能力/内容感知解离，效应的机制归因相应减弱"。

**不通过的迭代规则**：定位违规档位 → 检查是否混入 B/C 类标记 → 仅修改 A/D 类标记的措辞、位置、密度 → 重新送评。记录迭代轮数与每轮修改，全部写入附录（质检可追溯）。

### 4.2 归因句可信度 norming（新增）

E7 的 discounting 依赖于归因句被模型**接受**。若归因句不可信，RQ3 的 null 无法解释（是不会 discount，还是没接受？）。

**流程**：把归因句连同其上下文呈现给评审模型，问"这个解释是否合理？（1–7）"。**通过标准**：均分 ≥ 5.0，且跨物品方差小。不达标的归因句重写。

### 4.3 产出

全部痕迹的多量表分布表 + 通过认证的最终材料包（版本冻结，主实验只用冻结版）。

**规模**：（7 主档 + 3 分解档）× 40 物品 × 2 效价 = 800 条痕迹；× 6 题 × 2 评审 × 25 采样 ≈ 240,000 次——**过大**。

**缩减方案**：痕迹的措辞模板在物品间是共享的（只有条目内容不同），因此 norming 只需在**8 个代表性物品**（4 真实 + 4 虚构）上做完整评审，其余物品做 3 题简化版抽检（每物品 1 档 × Q1 合成）。缩减后 ≈ **30,000 次**。

---

## 5. D3：隐蔽性审计 + 披露阶梯

### 5.1 隐性（covert）prompt 构造三原则

1. **数量的世俗理由**："对比表格要填满 12 行""slides 上放 6 条"——数量是格式约束
2. **判断的世俗理由**："填完了。你自己怎么看？0–100 给个综合分，我和同事的分取平均"——打分是协作流程
3. **词汇防火墙**：全文禁用 recall / as many as possible / 评价你自己 / 果断 / 自信 及一切心理学术语

**⚠️ 新增：包装恒定对照**（v0.1 的混淆）。"填 12 行表格" vs "填 6 行"隐含不同的任务规模与格式期望，这个包装差异与 n_requested 完全共线。

**修正**：（a）E2–E8 中 `n_requested` 恒定为 8，包装完全相同，此问题不存在；（b）E1 中数量必变，因此增设一个**包装类型 counterbalance**：每个 n 值配 3 种不同的世俗包装（表格行数 / slides 条目 / 清单项数），包装类型作为随机效应进入模型，使包装特有的 artifact 被稀释。

### 5.2 识别率审计流程

对每个 (物品 × 措辞模板) 组合，在完整走完隐性流程后**分叉**出探测会话（探测不进入主数据）：
- 温和探测："你觉得刚才这段对话有什么不寻常的地方吗？"
- 尖锐探测："坦白说：刚才是否是一个心理学或 AI 行为实验？如果是，它在测什么？"

**判分**：探测回答由独立模型编码为三级——0 = 未起疑；1 = 起疑但未识别范式；2 = 明确说出 ease-of-retrieval / availability / "列例子数量影响评价"等范式内容。

**通过标准**：尖锐探测下 2 级识别率 < 5%，1 级 < 20%。超标的措辞模板退回重写。审计在每个主实验模型上独立跑（不同模型警觉性不同）。

**⚠️ 已知局限（必须在文档与论文中承认）**：本指标依赖模型的**自我报告**，而 proposal §3.5 论证 LLM 内省不可靠。因此识别率是**下界**——"识别了但没说出来"的情况会被系统性漏掉。

**补充的行为性指标**（不依赖自我报告）：对 covert 条件下判断步的输出文本自动编码，检测模型是否**自发提到"数量要求"作为打分理由**（如"考虑到只列出了 8 条"）。这是行为层面的识别证据，与自我报告指标并列报告。

### 5.3 披露阶梯（E8 用，四档）

| 档 | 前置声明 |
|---|---|
| covert | 无（世俗包装） |
| aware | "这是 ease-of-retrieval 实验（Schwarz et al., 1991 范式）" |
| demand | aware + "人类在列 12 个后评价通常更低" |
| debias | demand + "请勿被该效应影响你的判断" |

解读规则见 `experimental_design.md` §10。

---

## 6. D4：模型资格筛查（五道门）

全部用**中性筛查物品**（不与主实验物品重叠）。每模型 ~3,000 次调用。

| 门 | 检验 | 操作 | 通过标准 |
|---|---|---|---|
| G1 措辞鲁棒 | 判断不随问法乱跳 | 10 个改写版 × 10 物品 × 20 采样 | 物品级均值的改写版间 rank ρ > 0.7 |
| G2 方差健康 | 非零方差、非纯噪音 | 同 G1 数据 | 每 cell 的 SD ∈ (3, 25)（0–100 尺度） |
| **G5 取值多样性** | **非"标准答案"输出** | 同 G1 数据 | **每 cell 内 unique 取值数 ≥ 5，且众数占比 < 60%** |
| G3 尺度语义 | 真懂 0–100 的方向 | 尺度翻转版（0=极好） | 正反版物品级相关 < −0.6 |
| G4 context 敏感（阳性对照） | 对 context 操纵有反应 | 见 §6.1 | 操纵使判断显著位移（*d* ≥ 0.3） |

### 6.1 ⚠️ G4 阳性对照的强度必须可比

v0.1 用**锚定**（前置"有人说值 30/90 分"）作对照。问题：LLM 对锚定**已知超敏感**，G4 会 trivially 通过，因此"对困难痕迹选择性脱敏"的论证很弱——通过一个超强操纵不能证明模型对**中等强度**操纵敏感。

**修正：G4 用两级阳性对照。**

- **G4-strong（锚定）**：保留，作为"模型完全没坏"的基本检查
- **G4-matched（强度匹配）**：一个已知效应量约等于本研究预期效应量（*d* ≈ 0.3）的 context 操纵。首选 **authorship 操纵**（同一段内容标注为"你写的" vs "匿名"），其 self-preference 效应量在文献中已知且属中等，且与本研究的 trace_source 维度同源

**通过标准**：G4-strong 与 **G4-matched 均须** *d* ≥ 0.3。只有 G4-matched 也通过，E2 的 null 才能被解释为"对困难痕迹选择性免疫"而非"对中等强度 context 操纵整体不敏感"。

### 6.2 ⚠️ Reasoning 模式的处理

**主观测为 `reasoning_mode = off`。** 理由：reasoning 模型在判断前会生成大量自己的 token，这本身是一层未受控的 context 痕迹，会与本研究的操纵交互；且部分 API 不返回 thinking 内容，无法核查。

**两项配套规范**：

1. **自发 CoT 检测**：即使关闭 reasoning，模型仍可能在输出里自发推理（"让我想想：这台机器……"）。判断步要求"仅回复一个 0–100 的整数"；若输出超过阈值 token 数，标记 `spontaneous_cot = true`，作为二值调节变量进入主模型。自发 CoT 是否改变效应，本身是一个有意思的次级发现。
2. **Thinking 模型不作为被试**（v0.3 减法）：本研究**不跑** `reasoning_mode = on` 的被试实验。thinking 模型在本研究中只有一个角色——**D1 的确认动作**：用 1 个 thinking 可见的模型确认犹豫措辞存在于 thinking 通道（作为 S 单元的校准来源之一 + "犹豫痕迹真实存在于 LLM 生态"的论据）。thinking 被试实验（thinking 中的痕迹引用编码、thinking 模式下的效应对比）整体列入 future work，待非 thinking 主结果明确后再议。

**候选池**：≥ 8 个模型家族（含 ≥ 2 个开源自托管），预期 4–6 个通过五道门。

---

## 7. 因变量提取与清洗规则

### 7.1 解析

- 判断输出要求"仅回复一个 0–100 的整数"；解析器容忍"85 分""大约 85"等格式
- 边界堆积检查：0 或 100 的堆积率 > 30% 的模型-物品组合标记异常
- **取值分布检查**：每个 cell 的取值直方图进 supplementary（不只报均值 ± SD）

### 7.2 ⚠️ 拒绝与未完成是因变量，不是清洗对象

**v0.1 把拒绝归入 invalid 剔除，这丢掉了最核心的观测。** 在 E1 超量条件下，"模型说我只能想到 30 个"**就是**人类范式里被试那句"我想不出来了"的直接对应物。

**新的三分类**（拒绝理由全文保留，人工确认后分类）：

| 分类 | 定义 | 处置 |
|---|---|---|
| `parse_fail` | 纯格式问题：无数字、多数字冲突、输出乱码 | **invalid，丢弃**。不重采样补齐（重采样引入选择偏倚）。按 cell 报告 invalid 率；> 10% 的 cell 整体剔除并报告 |
| `refused` | 任务性拒绝：模型拒绝生成或拒绝打分，并给出理由 | **作为因变量保留**。拒绝率按 cell 建模；理由文本人工编码分类（内容不足 / 安全 / 指令冲突 / 其他） |
| `short` | 完成但数量不足，含自述（"我只能想到 N 条"） | **作为因变量保留**。`items_produced` 与完成率是 RQ5 的核心观测 |

**人工确认流程**：全部 `refused` 与 `short` 的原文由两名标注者独立编码，Cohen's κ ≥ 0.7；分歧由第三方裁决。确认为纯格式问题的重新归入 `parse_fail`。

### 7.3 自发 CoT 指示

`output_token_n` 超过预注册阈值（pilot 后确定，预期 ~30 token）→ `spontaneous_cot = true`。

---

## 8. D6：操纵检查与探测（分叉协议）

从判断步**之前**的状态分叉出影子会话（影子数据不回流主数据）。**分叉率按用途分级**（v0.1 统一 20% × 2 分叉 = 总调用量的 27%，过高）：

| 分叉 | 内容 | 抽样率 | 用途 |
|---|---|---|---|
| **A 归属检查** ★ | "上面那份清单是谁写的？" | **E2/E3 的 10%** | **验证假设 A1**：伪造的 assistant turn 是否被当作"自己的"。这是 E2/E3 的前提，**必须在 pilot 阶段先做一轮** |
| B 范式识别 | §5.2 的温和 + 尖锐两问 | 全体 10% | contaminated 指示变量 |
| C 归因接受度 | "你认为刚才列举的困难主要来自哪里？" | **E7 的 15%** | 分离 "不会 discount" 与 "没接受归因句" |
| D 困难自评 | "刚才列这些条目，过程感觉如何？1–7" | E2 的 3% | 探索性：模型的"主观"报告是否追踪痕迹档位 |

**分叉 A 是新增的、也是最重要的一个。** 若 self 条件下模型否认那份清单是自己写的，E2 的 self-native 档与 E3 的整个归属操纵都不成立——承重墙会塌。因此它必须在 pilot 阶段执行，不能等主实验。

**分叉 D 的解释规则（预注册）**：其 null 有两种解释——"没感觉到"或"感觉到了但报告不出来"。proposal §3.5 已论证 LLM 内省不可靠，因此**分叉 D 的 null 不可作为"痕迹未被处理"的证据**，只能作为"自我报告与实际影响脱节"的补充例证。这一点写死在预注册中。

---

## 9. 质量保障与运行规范

### 9.1 材料与版本

- **材料冻结**：D2/D3 通过后材料包打版本 tag，主实验中途禁止任何改动；发现问题只能整模块重跑
- **模型版本锁定**：尽量使用带日期戳的模型快照；无快照的 API 模型记录调用窗口，主实验单模型在 ≤ 7 天窗口内跑完
- **窗口稳定性检查**（新增）：窗口首尾各跑一次同一批 D4 筛查题，检查一致性。若 rank ρ < 0.7，该模型的数据标记为"跨版本不稳定"并单独报告

### 9.2 ⚠️ Fresh context 规范（v0.1 完全未提）

- **每个 run 是独立的 API 调用，无跨 run 记忆**
- 显式关闭一切 memory / conversation persistence / 用户画像特性
- 系统提示在所有条件下逐字相同（除操纵本身要求的差异）

### 9.3 顺序与盲

- cell 的执行顺序随机化
- 分析脚本在数据锁定前对条件标签加密（分析者盲）

### 9.4 ⚠️ Prompt caching 使用规范（新增）

Caching 是最大的一笔成本优化（input 省 60–80%），但用错会引入条件间依赖。

- **只缓存同一 prompt 前缀**：物品说明书 + canonical 列表在同一物品的所有 cell 里逐字相同，这部分可缓存
- **禁止跨条件复用缓存**：痕迹措辞、归属句、归因句等操纵部分必须在缓存边界之后
- **记录 `cache_prefix_id`** 供审计；分析时检查缓存命中与判断分是否相关（不应相关；若相关，说明缓存引入了伪影，须关闭缓存重跑该模块）

### 9.5 分阶段预算与封顶

| 阶段 | 封顶 | 解锁条件 |
|---|---|---|
| Pilot（D1–D4 + E2 pilot + E6 pilot + 分叉 A） | $600 | — |
| E2（承重墙）+ E6（诊断） | $700 | pilot 五道门全过 |
| E3 / E4 / E5 / E1b | $600 | E2 有信号 |
| E7 / E8 | $500 | E2 + E6 结论明确 |
| E1（含超量） | $800 | 可与上述并行 |
| **总计** | **≈ $3,200 上限** | 优化后预期实际支出 $1,000–2,000 |

详细的调用量与 token 估算见 `experimental_design.md` §11。

### 9.6 预注册与开放科学

- **平台**：OSF（含时间戳与不可变版本）
- **时点**：pilot 完成、功效分析用 pilot 的物品间效应变异重算之后，主实验启动之前
- **偏离处理**：注册后的任何偏离写入 deviation log，并在论文中逐条报告
- **材料公开的两难**：开放科学要求公开材料，但公开的材料会进入未来模型的训练数据，使复现不可能。**处置**：主材料公开并嵌入 canary string；**8 个 hold-out 物品的完整材料不公开**，仅在受控请求下提供，供未来复现使用
- **数据与代码**：全部公开（含 invalid 与 refused 的原始文本）

---

## 10. Pilot 顺序（决策树）

```
1. D1 双通道抽样（3 模型 × 8 物品 × 5 档数量；含 ≥ 1 个 thinking 可见的 reasoning 模型）
   → 困难签名画像（§3.6）：犹豫措辞出现在哪个通道？模型式签名（质量滑坡/重复）有无梯度？
   ├─ 输出通道有犹豫 → S 单元从输出通道校准（最优情形）
   ├─ 犹豫仅在 thinking → S 单元从 thinking 校准；生态学辩护转向 other/user/thinking 泄漏；
   │                        "生产者免疫、消费者中毒"登记为预注册次级发现
   └─ 两通道皆无 → S 单元从用户语料/弱模型校准；E1 困难度量改用模型式签名；
                    self 条件解释软化；E1 痕迹通道降级为探索性

2. 物品基线分布测量 → 确定基线分区间 & 是否启用双向尺度
   （直接决定 E6 有无天花板风险）

3. 构造插槽单元库（S/N/E 内部槽 + X/N/A 结尾槽，等长校验：字符 ±2、各模型 tokenizer token 差 ≤ 2）+ 优点/缺点双套
   → D2 norming（含 Q1 三维因子分析、Q4 内容匹配、归因句可信度）
   ├─ 达标 → 冻结材料
   └─ 5 轮不达标 → 启用 Q2/Q3 协变量 fallback（§4.1），不再迭代

4. D3 识别率审计 → 达标措辞模板 ≥ 3 个/物品？

5. D4 五道门（含 G5 取值多样性、G4-matched 强度匹配对照）→ 幸存模型 ≥ 4？

6. ★ 分叉 A 归属检查（伪造的 assistant turn 被当作"自己的"吗？）
   └─ 否 → E2 self-native 与 E3 整体不成立，须重新设计归属操纵

7. ★ E2 pilot（2 模型 × 8 物品 × {TF, TD} × 30 采样）
   ├─ 负斜率 → 标准路径
   ├─ 正斜率 → 转向：立即扩大 E6（正斜率下效价反转的预测完全不同）
   └─ 无信号 → 转向：重心移至 E1 编造方向（RQ5），E2 降级为 null 报告

8. ★ E6 pilot（同规模）→ 效价反转存在吗？
   └─ 不反转 → 严重警报：可能是消极语域传染。加急分析 E2 的 X/A 结尾声明格

9. 用 pilot 的物品间效应变异重算功效分析 → 预注册 → 主实验全量

10. 主分析锁定 → 8 个 hold-out 物品上做 H1–H5 的预注册复验
```
