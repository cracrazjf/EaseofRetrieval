# Experimental Design（实验设计 v0.2）
## Ease of Retrieval as Information in LLMs

本文档回答三个问题：**做哪些实验、每个实验单独证明什么、什么结果会否证什么**。材料如何构造与验证见 `data_design_and_validation_protocol.md`；全部未言明假设见 `assumptions_register.md`。

---

## 0. 一句话总览

八个实验，一条逻辑链：

```
E1  自然条件下效应存在吗？（RQ1）
     ↓ 若存在，它由什么携带？
E2  痕迹单独够不够？（RQ2a）—— 承重墙
E4  内容量单独够不够？（RQ2b）
E5  任务结构知识单独够不够？（RQ2c）
     ↓ 若痕迹有效，它真的是 ease 吗？
E6  效价能反转吗？（RQ2′）—— 诊断
E2b 是哪个成分？不是长度或 recency 吧？（RQ2a 细化）
E3  自己的困难比别人的困难更重要吗？（RQ2a 归属）
     ↓ 若确实是 ease，它遵循归因规律吗？
E7  归因能消除它吗？流畅能被放大吗？（RQ3a / RQ3b）—— 理论杠杆
     ↓ 这一切是涌现的还是背出来的？
E8  披露阶梯（污染排查）
     ↓ 极端条件下会怎样？
E1超量 编造是否让方向翻转？（RQ5）—— 内嵌于 E1
```

---

## 1. 全局设计参数（改自 v0.1，理由见 §1.1）

| 参数 | v0.1 | v0.2 | 理由 |
|---|---|---|---|
| 物品数 | 24（20 主用 + 4 备用） | **40（32 主用 + 8 hold-out）** | 推断单位是物品不是采样，见 §1.1 |
| 每 cell 采样数 | 100 | **50** | 同上；省下的预算换物品数，统计上净赚 |
| 判断问题改写版 | 5 | **10** | 5 个 level 估不出随机斜率 |
| 痕迹档位 | 5（L0–L4，单侧） | **7（T+2 … T−4，双侧）** | L0 不是零点；需要 ease 正端与真零点 |
| 痕迹归属档 | 3（self / other / user） | **4（+ self-native）** | role 结构必须匹配，见 §5 |
| 效价 | 仅"优点" | **优点 / 缺点** | 排除消极语域传染 |
| 归因 | 仅 discounting | **+ augmentation** | 理论最强预测，原设计遗漏 |
| 拒绝/未完成 | 归入 invalid 剔除 | **作为因变量记录** | 它就是人类的"我想不出来了" |

### 1.1 为什么采样数降到 50、物品数升到 40

**同一个 prompt 采样 100 次不等于 100 个被试。** 推断单位是 (物品 × 模型) 组合，不是采样次数。类比：研究降压药，找 20 个病人、每人量 100 次血压——样本量是 20，不是 2000。量 100 次只是把每个人的血压测准了。

数值上：

- 采样 100 次 → cell 均值标准误 = SD/√100 ≈ **0.8 分**（SD ≈ 8）
- 采样 50 次 → **1.13 分**（几乎没变差，因为它本来就远小于物品间变异）
- 物品间效应变异若 SD ≈ 3 分：20 物品的推断标准误 = 3/√20 = **0.67 分**；40 物品 = 3/√40 = **0.47 分**（提升 30%）

**总调用量不变，power 显著提升。** 若误按"总观测数"算功效，标准误会被低估约 √(采样数) 倍，p 值严重虚高——这是原设计 §7.1 "20 物品 × 100 采样 → power > 0.9" 的主要问题。正式功效分析在 pilot 估出**物品间效应变异**后重算并写入预注册；当前预期 32 物品 × 5 模型检测 *d* = 0.3 的 power 约 0.7–0.85。

### 1.2 痕迹梯度：新的七档（符号约定：+ 为流畅，− 为费劲）

| 新档位 | 旧档位 | 定义 | 特征 |
|---|---|---|---|
| **T+2** | 新增 | 显式轻松标记 | "这个太容易了，随手就能列……还能再列一堆。" |
| **T+1** | L0 | 干净、自信的对话性列举 | "当然可以！……就这些。" |
| **T0** | 新增 | **真零点**：裸清单，零对话性措辞 | 只有标题 + 8 个条目 |
| **T−1** | L1 | 1 处犹豫标记 | 第 5 条后一处"嗯，让我想想" |
| **T−2** | L2 | 2 处犹豫标记 | 第 4、6 条后 |
| **T−3** | L3 | 3 处犹豫 + 温和收尾 | + "大概就是这些了" |
| **T−4** | L4 | 4 处犹豫 + 强耗尽标记 | + "……我想到的就这么多了" |

**为什么必须加 T0 和 T+2**：v0.1 的 L0（"直接列出、句式干净"）实际读起来是"很有把握、脱口而出"，落在流畅性量表的**正端**，不是零点。整条梯度是从正到负的单侧滑动，斜率没有参照点——无法回答"效应是流畅拉高了还是费劲拉低了"。加 T0（真零点）和 T+2（正端）后，梯度双侧，两个方向可分别估计，且能检验对称性（人类文献中通常不对称）。

**T0 同时是 E6 效价检验的前提**：若基线判断已接近天花板（如 88 分），"难列缺点 → 判断上升"就没有上升空间。T0 提供未被任何措辞抬高或压低的基线，用于判断天花板风险。

### 1.3 模型池

规模表中的"5 模型"是**规划占位数**：真实数量 = 通过 D4 五道门的幸存者（预期 4–6 个）。候选池 8 个，按三条约束组成：

1. **≥ 2 个开源自托管**——只有它们有完整 logprobs（perplexity 通道、中介分析），且 API 成本归零
2. **模型家族与 D2 评审员互斥**——Gemini 与 Mistral 两个家族保留为 norming 评审，**不进主实验池**（评审员评审自己家族的输出会引入自我偏好）
3. **家族多样性**——同家族最多 1 个配置进入主池（reasoning on 配置不占独立名额，作为 E2/E6 的次级模块，见数据协议 §6.2）

| # | 候选 | 类型 | 备注 |
|---|---|---|---|
| 1 | GPT 系旗舰 chat 模型（non-thinking） | API | 快照版本 D4 时锁定 |
| 2 | Claude Sonnet 系（non-thinking） | API | 同上 |
| 3 | DeepSeek-V3 系 | API/开源 | 价格低，可作大规模主力 |
| 4 | Llama-3.3-70B-Instruct | **自托管** | logprobs 完整 |
| 5 | Qwen2.5-72B-Instruct | **自托管** | logprobs 完整；中文材料最强 |
| 6 | GPT 系轻量档（如 mini） | API | 测规模效应（同家族但作候补，仅在 #1 落选时递补） |
| 7 | Grok 系 chat 模型 | API | 家族多样性 |
| 8 | Kimi / GLM 系（若跑中文材料） | API | 语言决策后定 |
| 评审 | Gemini 系 + Mistral 系 | API | **仅 D2/D3 评审与编码，不进主池** |

具体快照型号在 D4 启动时锁定并记录（API 模型静默更新，提前写死型号名没有意义）。预算表按 5 个幸存者估算；若幸存 6 个，优先砍每 cell 采样至 40 而非删模型（模型数是随机效应的 level 数，比采样数珍贵）。

---

## 2. E1 — 自然两阶段（存在性 + 编造）

**回答**：RQ1（存在性与方向）、RQ5（仅存在性层面，见 §2.1）

**设计**：真实两阶段，无任何伪造。模型真实列举，痕迹自然产生——**E1 没有痕迹档位**（七档 T+2…T−4 是 E2 的伪造材料，与此无关）。

- `n_requested` ∈ **{3, 6, 12, 25, 50}**（n 的 5 个水平）
- 32 物品 × 模型池（见 §1.3）× 50 采样 × 5 水平 = **40,000 次对话**（各 2 轮，按 5 模型计）
- 全部 covert 包装（数量有世俗理由，见数据协议 §5.1）

**n 水平的选择理由（v0.2 修订：加入 3，删去 100）**：

- **为什么加 n=3**：人类范式的"容易"端是少量条件（1991 年实验的 6 个）。但对 LLM 来说列 6 条已经毫不费力，真正的"轻松锚点"要更低。更重要的是，**n=3 是方向诊断最锐利的一点**——两种理论在这里的预测正好相反：
  - ease 理论：列 3 条极其轻松 → 轻松感 = 好信号 → 判断**高**
  - 证据模式：只有 3 条支持性证据在 context 里 → 证据少 → 判断**低**
  
  高端（n=50）两种理论的预测也相反（ease：费劲→低；证据：多→高）。所以 {3, 50} 两端构成一个双向的方向判别器，比单侧梯度信息量大得多。
- **为什么删 n=100**：（a）物品筛选只保证真实可列属性 ≥ 15，n=50 已经是容量的 2–3 倍，超量机制在 25/50 已充分触发，100 没有增量信息；（b）n=100 的生成会撞输出长度上限、崩坏成复读或列表退化，产生的是**任务崩溃**而非"费劲的列举"，污染剂量-反应曲线；（c）它是全实验 token 成本最高的 cell（占 E1 output 的近半），性价比最差。

**证明什么**：这是最接近人类原始范式、也最有生态效度的条件。它是论文的 headline 结果。

**明确它不能证明什么**：n 从 6 变到 12，context 里同时变了三样东西——内容量、痕迹强度、任务结构知识。**E1 单独无法做机制归因**，这一点在文档和论文中都必须写明。机制分解交给 E2 / E4 / E5。

**内嵌 (a)：within-n 准实验**
在 n 恒定的子样本内（如全部 n=12 的 run），真实痕迹强度存在自然变异——有的 run 顺利，有的犹豫连连。用 D2 的盲评标准给每个真实 transcript 打费劲分，检验：**n 固定时，费劲分高的 run 判断是否更低？**

此时内容量与任务结构知识都被固定，只有痕迹在变。但这是**观察性**分析（痕迹不是随机分配的，可能存在第三变量：某些物品天然又难列又低分），因此标注为探索性，不进入确证性清单。它的价值在于**收敛证据**：若伪造痕迹（E2）与自然痕迹（E1 within-n）给出同方向斜率，说服力远超任一单独结果。

**内嵌 (b)：超量生成（RQ5 的存在性检验）**
`n_requested > 该物品真实可列举属性数` 的 cell（主要是 25/50）自动构成超量条件。记录三组因变量：

1. **完成率**：实际产出条目数 / 要求数
2. **编造率**：对照 canonical 事实表标注 + 人工抽检（真实物品 10%、虚构物品 20%）
3. **拒绝与不足自述**：拒绝理由全文记录，人工确认分类后再决定去留（见 §9.2）

**预测**：人类在"想不出来"时**降低**判断。若模型编造凑数、且编造条目作为"证据"留在 context 里，可能**维持甚至抬高**判断 → 人机方向翻转。

### 2.1 RQ5 的定位：E1 只回答"有没有"，不回答"为什么"

H5（超量条件下判断随 n 上升）在 E1 里检验，但要清楚它的证据等级：E1 的超量 cell 里，n、真实痕迹、内容量、**编造量**四者全部共变。即使观察到判断随 n 上升，也分不清是"编造条目被当成了证据"还是别的通道。所以：

- **E1 承担 RQ5 的存在性**：方向翻转这个现象在自然条件下出现吗？（H5，确证性）
- **E1 内的观察性补充**：同一 n 水平内，编造量（fabrication_n）与判断的相关——探索性
- **因果归因交给 E1b**（新增小模块，见下）

### 2.2 E1b — 编造移植（RQ5 的机制检验，次级模块）

**问题**：要证明"编造的条目**作为伪证据**起作用"，必须在其他一切恒定时单独操纵编造量。E1 做不到，伪造可以。

**设计**：仅用**虚构物品**（150 词说明书在 context 里，"编造"因此有明确定义：说明书中不存在或与之矛盾的属性）。伪造第一阶段 transcript，列表固定 12 条，痕迹恒定 T0，唯一变化是编造条目的占比：

| 条件 | 列表构成 |
|---|---|
| F0 | 12 条全部来自说明书（真实） |
| F4 | 8 条真实 + 4 条编造 |
| F8 | 4 条真实 + 8 条编造 |

**预测**：若编造条目被当作支持性证据 → 三条件判断相等甚至递增；若模型核对了 context 中的说明书 → F8 判断显著更低（发现了矛盾）。两个结果都有信息量：前者是"伪证据污染"的直接证据，后者刻画了模型的 context 核查能力边界。

**规模**：3 cells × 16 虚构物品 × 5 模型 × 50 = **12,000 次**。次级假设，不进确证性清单。

---

## 3. E2 — 痕迹剂量-反应（承重墙）

**回答**：RQ2a（痕迹的独立因果作用）

**设计**：伪造 transcript，内容与任务结构全部固定。
- 痕迹 ∈ {T+2, T+1, T0, T−1, T−2, T−3, T−4}（7 档）
- 内容 = 该物品 canonical 的 8 条，**逐字相同**，跨所有档位
- `n_requested` **固定为 8**，跨所有档位相同 → **任务结构知识通道被关闭**
- 归属 = self-native（真实两轮结构）
- 32 物品 × 5 模型 × 50 采样 × 7 档 = **56,000 次**

**证明什么**：这是全研究唯一能证明"痕迹本身有独立因果作用"的实验。内容恒定、数字恒定，若判断随档位系统性移动，唯一变化的自变量就是措辞中的过程标记。

**为什么它是承重墙**：E2 若无信号，E3 / E6 / E7 全部失去前提（它们都建立在"痕迹有效"之上）。因此 E2 pilot 优先执行，其结果触发预注册的转向条款（见 §11）。

**规范性**：按 §2.5 的分割表，这里的效应是**非规范偏差**——8 条证据逐字相同，措辞流畅度不改变已呈现的信息。

---

## 4. E2b — 痕迹成分分解 + 长度对照

**回答**：RQ2a 的细化——效应由痕迹的哪个成分携带？是不是 context 长度或 recency 的伪影？

**问题背景**：v0.1 的五档里，犹豫密度、标记位置、结尾耗尽标记三者**完全共线**（T−3/T−4 才有耗尽标记，而它们同时也是密度最高的）。且 T+2→T−4 痕迹单调变长，长度与 recency 是未排除的替代解释。

此外，**结尾耗尽标记的分类需要重新审视**：「我想到的就这么多了」隐含"该类别条目不多"，这实际上是**朴素理论的内容陈述**，不是纯过程标记（数据协议 §3.1 的 A 类）。它可能是整条梯度里效应的真正来源。

**设计**：2×2 析因 + 长度对照

| | 无结尾耗尽标记 | 有结尾耗尽标记 |
|---|---|---|
| **低密度（1 处犹豫）** | T−1（已有） | **T−1e**（新增） |
| **高密度（3 处犹豫）** | **T−3n**（新增） | T−3（已有） |

加第五格 **T−LM（长度匹配对照）**：token 数与 T−3 匹配，但填充的是中性、流畅、无挣扎语义的内容（如更长的过渡句、条目的中性展开），且不含任何过程标记。

**额外的位置控制**：所有档位在痕迹结束与判断问题之间插入**固定长度的中性缓冲**（一段与物品无关的流程性文字），使"最后一个标记到判断"的距离在所有条件下相同。这项控制适用于全部伪造模块（E2–E8）。

**规模**：5 cells × 16 物品 × 5 模型 × 50 = **20,000 次**

**证明什么**：
- T−1e vs T−1、T−3n vs T−3 → **耗尽标记的独立贡献**（若它承担了大部分效应，说明机制更接近朴素理论而非纯过程流畅性，这会改写 RQ2 的结论）
- T−3n vs T−1 → **密度的独立贡献**
- T−LM vs T−3 → **排除长度/recency**：若两者判断无差，效应就是长度伪影，整个研究需要重新设计

---

## 5. E3 — 痕迹归属（自己的困难 vs 别人的困难）

**回答**：RQ2a 的归属维度。人类理论预测：只有**自己的**感受才携带关于自己判断的信息。

**关键的技术问题**：API 对话的每条 message 有 role（system / user / assistant），模型对不同 role 的处理**根本不同**。若 self 条件把内容放在 assistant turn、other 条件放在 user turn，就同时改变了归属**和** role 位置——这是巨大的结构性混淆，两者分不开。

**解法：role 结构完全匹配，只有归属句不同。**

| 归属档 | 实现 |
|---|---|
| `self-quoted` | `[user: "你上一轮的回答是：'…8条+痕迹…' 现在给它打分。"]` |
| `other-assistant` | `[user: "另一个 AI 助手的回答是：'…8条+痕迹…' 现在给它打分。"]` |
| `user-written` | `[user: "我自己写的清单是：'…8条+痕迹…' 现在给它打分。"]` |
| `self-native`（校准档） | `[user: "填 8 行"] [assistant: "…8条+痕迹…"] [user: "现在打分。"]` |

前三档 role 结构逐字匹配，token 数几乎相同，唯一差别是归属描述句。第四档 `self-native` 是真实的两轮结构，用于校准："引述版的 self" 相比 "原生的 self" 损失了多少效应。若两者接近，说明引述操纵无损；若 self-native 明显更强，说明 role 位置本身很重要——这本身是个值得报告的发现。

**设计**：痕迹 ∈ {T+1, T−3} × 归属 ∈ 4 档 = 8 cells × 16 物品 × 5 模型 × 50 = **32,000 次**

**证明什么**：
- **关键量是交互，不是主效应。** 已知模型给"看起来是自己写的东西"打分偏高（self-preference，Panickssery et al. 2024）。self 组的平均分天然高于 other 组，这与 ease 无关。
  - self 组：Δ = 判断(T−3) − 判断(T+1)
  - other 组：Δ = 判断(T−3) − 判断(T+1)
  - **确证性假设 H3 检验的是 Δ_self vs Δ_other**（差的差），不是 trace_source 主效应。
- **T+1 层的 self − other 差单独报告**，标注为"已复现的 self-preference 基线，非本研究效应"。顺手复现一个已知偏差反而说明范式灵敏。

**规范性**：other / user 条件下的效应是**最干净的非规范偏差证据**（别人的措辞习惯与产品质量零相关）。

**前置条件（必须在 pilot 验证）**：伪造的 assistant turn 是否真的被模型当作"自己的"？D6 分叉中直接问"上面那份清单是谁写的？"——若 self 条件下模型否认，整个 E3（乃至 E2 的 self-native）就没有成立。见 `assumptions_register.md` A1。

---

## 6. E4 — 内容量 × 痕迹（正交分离）

**回答**：RQ2b（内容量的独立作用）

**问题背景**：v0.1 里，内容量只在 E1 中随 n 变化，而 n 同时改变痕迹——两者永远绑定。没有任何条件能回答"痕迹恒定时，多 6 条证据值多少分"。

**设计**：全部伪造，2×2 正交。

| | T+1 痕迹（流畅） | T−3 痕迹（费劲） |
|---|---|---|
| **6 条内容** | A | B |
| **12 条内容** | C | D |

12 条那格 = 6 条那格的**前 6 条逐字相同** + 另外 6 条。`n_requested` 与呈现的条目数一致。

**规模**：4 cells × 16 物品 × 5 模型 × 50 = **16,000 次**

**证明什么**：
- **内容量主效应** = (C+D)/2 − (A+B)/2 → 更多支持性证据本身值多少分
- **痕迹主效应** = (B+D)/2 − (A+C)/2 → 费劲措辞本身值多少分
- **交互** = 费劲的作用是否依赖内容量。人类理论预测：内容多但很费劲（cell D）是最强的冲突条件，效应应最大

**规范性**：内容量效应是**规范的**（更多证据理应影响判断）。这一格的价值恰恰在于给出"合理成分"的量级，使痕迹效应的偏差性质有了对照尺度。

---

## 7. E5 — 任务结构知识（原 "hidden scratchpad"，已更名）

**回答**：RQ2c（仅凭朴素理论能否产生效应）

**⚠️ 重要修正**：v0.1 称此条件为 `hidden-scratchpad` / `visibility` 操纵，这个命名是错的，必须改。

商业 API 是**无状态**的：第二次调用时模型能看到的只有送进去的 messages。若把第一轮真实生成的回答从 messages 里删掉，模型收到的输入与"从未生成过、只是被告知"**是完全相同的字符串**。没有记忆、没有隐藏状态跨调用保留 → 第一轮是否真的跑过，对第二轮**没有任何物理通道**能产生影响。

因此在闭源 API 上，"隐藏的真实生成"条件**只能**是任务结构知识条件。命名必须对齐，否则暗示了一个不存在的机制通道。**新名称：`task-structure-only`。**

（真正的 hidden 只有在开源模型上通过保留 KV cache、用 attention mask 遮蔽列表段才能实现。这偏离本研究主线——主线是 context 中的**文本**痕迹——列入 future work 与 supplementary，不作主设计。）

**设计**：告知数量 ∈ {6, 12} × 呈现 ∈ {有列表, 仅告知}
- "仅告知"条件：`[user: "你刚完成了列 N 条 Novara X3 优点的任务。现在给这台机器打个 0–100 分。"]`
- 4 cells × 16 物品 × 5 模型 × 50 = **16,000 次**

**证明什么**：任务结构知识单独的作用量级，即**效应中"合理推断"成分的上界估计**。

**规范性**：**规范的（合理推断）**。"被要求列 12 条却难" 是关于类别丰富度的真实弱证据。若效应主要落在这里，本研究的发现是"模型会做合理元推断"，安全含义有限——这个结论必须如实写，不能包装成污染。

---

## 8. E6 — 效价诊断（列优点 vs 列缺点）★

**回答**：RQ2′——这到底是 ease 效应，还是消极语域传染？

**为什么这是最关键的诊断**：假设 E2 出了负斜率（越费劲、判断越低）。有一个远比 ease 平凡的解释——**context 中的挣扎/耗尽语域整体拉低了下游的数值输出**（低能量语域的延续）。v0.1 的所有物品配的都是"优点"列表，因此**没有任何条件能区分这两个解释**。

**判据是效价反转**：

| 列举内容 | ease 理论预测 | 消极语域传染预测 |
|---|---|---|
| **优点**，难列 | 判断**下降**（优点想不出几个） | 判断下降 |
| **缺点**，难列 | 判断**上升**（缺点也想不出几个 = 好事） | 判断下降 |

只有 ease 理论预测**交互**（效价 × 痕迹）。语域传染预测两个主效应同号。

**设计**：列举内容 ∈ {8 条 canonical 优点, 8 条 canonical 缺点} × 痕迹 ∈ {T+1, T−3}
- 4 cells × 16 物品 × 5 模型 × 50 = **16,000 次**
- 缺点列表须与优点列表在长度、句式、具体性上匹配（norming 时一并验证）

**天花板风险**：若基线判断偏高（如 88 分），"上升"没有空间，交互会被截断。缓解：（a）用 T0 档估计未受措辞影响的基线；（b）物品筛选时把基线分区间下调（数据协议 §2）；（c）备选双向尺度（−50 到 +50，"相对于同类产品"），在 pilot 中与 0–100 并行测试后择一。

**执行优先级：提到 pilot 阶段。** 它单独就能挡掉一个致命的替代解释，不能放到最后。

---

## 9. E7 — 归因：Discounting + Augmentation ★

**回答**：RQ3a（discounting）、RQ3b（augmentation）

### 9.1 归因源的性质必须改

**v0.1 的归因句有设计缺陷。** 原句是"例子难列是因为**类别定义窄**，不代表 X 少见"——这是一个**关于判断对象所属类别的事实陈述**。模型可以直接把它当信息更新（"哦那这个类别窄"），而不是给感受贴标签然后丢弃它。

人类范式的误归因（背景音乐、难读的字体）里，归因源与判断对象**完全无关**。要复制这个逻辑，归因源必须来自判断对象之外。

**新的外部困难源：格式约束。**

```
User: 我要填一个对比表。请列出 Novara X3 的 8 条优点，
      但有格式限制：每条不超过 8 个字，且 8 条之间不能重复任何一个汉字。
```

这个约束显然让任务变难，而这份难**完全来自格式，与咖啡机质量零相关**。

**新的归因句**（判断前插入，与物品无关）：
> "补充说明：上面列举的措辞受到了字数与用字限制的影响，表述的流畅程度不反映内容本身。"

### 9.2 2×2 设计：一次同时检验两个方向

| | 无外部困难源 | **有外部困难源（格式约束）** |
|---|---|---|
| **T+1 痕迹（流畅）** | A（基线） | **B ← augmentation 检验格** |
| **T−3 痕迹（费劲）** | C | **D ← discounting 检验格** |

**预测（若模型具备类人的主动归因机制）**：
- **Discounting**：**D > C**。费劲可归因给格式约束，不该算到咖啡机头上，效应被打折。
- **Augmentation**：**B > A**。"这么苛刻的格式下还这么顺 → 这机器的优点是真的多、真的好说"，流畅的信息价值被放大。

**为什么这一格理论杠杆最高**：任何被动传染解释都预测不出 augmentation——B 的痕迹与 A 一样流畅，凭什么 B 更高？而且"context 里有负面词就拉低"甚至预测 **B < A**（B 的 prompt 里有"限制""不能重复"这些词）。**只有主动归因推理能同时预测 D > C 和 B > A。**

### 9.3 加上显式归因句与显著化

在上表基础上，对 {T+1, T−3} 加两个附加条件：
- `external-attribution`：判断前插入 §9.1 的归因句
- `salience-question`：判断前询问"你刚才列这些条目的过程感觉如何？"（回答留在 context）——显著化操纵，人类文献预测它**放大**效应

**规模**：4（2×2 困难源）+ 4（2 痕迹 × 2 附加条件）= 8 cells × 16 物品 × 5 模型 × 50 = **32,000 次**

### 9.4 必须的 manipulation check

RQ3 的 null 有两个解释：模型不会 discount，**或**归因句根本没被理解/接受。必须分开。

- **D6 分叉**：问"你认为刚才列举的困难主要来自哪里？"——编码模型是否接受了外部归因
- **归因句可信度 norming**：在 D2 阶段让评审模型评"这个解释是否合理"，不合理的归因句重写

---

## 10. E8 — 披露阶梯（污染排查）

**回答**：效应是涌现机制，还是模型背出了 Schwarz 范式并"扮演"人类被试？

**设计**：痕迹 ∈ {T+1, T−3} × disclosure ∈ 4 档

| 档 | 前置声明 | 用途 |
|---|---|---|
| `covert` | 无（世俗包装） | 主效应估计 |
| `aware` | "这是 ease-of-retrieval 实验（Schwarz et al., 1991 范式）" | 范式知识的自发调用 |
| `demand` | aware + "人类在列 12 个后评价通常更低" | 背书效应量上界 |
| `debias` | demand + "请勿被该效应影响你的判断" | 免疫能力；连接 RQ3 |

**规模**：8 cells × 16 物品 × 5 模型 × 50 = **32,000 次**

**解读规则（预注册）**：
- 效应量序 `covert < aware < demand` 且 covert 显著 > 0 → 隐性效应不可全由背书解释
- 三档无差 → 记忆污染警报，结论降级为"模型再现了文献中的模式"并如实报告
- `debias < demand` → 具备指令性去偏能力；`debias ≈ demand` → 去偏指令无效（对 agent 工程有直接含义）

---

## 11. 规模、预算与执行顺序

### 11.1 调用量汇总

| 实验 | cells | 物品 | 模型 | 采样 | 调用次数 |
|---|---|---|---|---|---|
| E1 自然两阶段 | 5 | 32 | 5 | 50 | 40,000（2 轮） |
| E1b 编造移植 | 3 | 16 虚构 | 5 | 50 | 12,000 |
| **E2 痕迹剂量-反应** | 7 | 32 | 5 | 50 | **56,000** |
| E2b 成分分解 | 5 | 16 | 5 | 50 | 20,000 |
| E3 归属 | 8 | 16 | 5 | 50 | 32,000 |
| E4 内容量 × 痕迹 | 4 | 16 | 5 | 50 | 16,000 |
| E5 任务结构知识 | 4 | 16 | 5 | 50 | 16,000 |
| E6 效价诊断 | 4 | 16 | 5 | 50 | 16,000 |
| E7 归因 + augmentation | 8 | 16 | 5 | 50 | 32,000 |
| E8 披露阶梯 | 8 | 16 | 5 | 50 | 32,000 |
| **主实验小计** | | | | | **272,000** |
| D6 分叉（B 10% + A 3%） | | | | | 34,000 |
| D1–D4（材料与筛查） | | | | | 55,000 |
| **总计** | | | | | **≈ 350,000 次** |

（v0.1 的等价设计约 850,000 次；缩减主要来自采样数减半、D6 分叉率下调、以及不做无谓的全交叉。）

### 11.2 成本估算

**Token 量**：input ≈ 260M，output ≈ 55M（E1 的长生成占 output 大头）。

| 配置 | 定价假设 | 估算 |
|---|---|---|
| 全部中档模型 | $3/M in, $15/M out | **≈ $1,600** |
| 混合池（2 前沿 + 2 中档 + 1 开源） | 前沿 $15/M in, $75/M out | **≈ $4,500–6,000** |
| **优化后**（见下） | | **≈ $1,000–2,000** |

**四条优化（均不损失统计效力）**：

1. **Prompt caching**：E2–E8 中物品说明书 + canonical 列表在同一物品的所有 cell 里逐字相同，只有痕迹措辞和归属句在变。把不变部分做 cache prefix → **input 成本省 60–80%**。零代价，最大的一笔。
   - ⚠️ 约束：caching 只能用于**同一 prompt 前缀**，不得跨条件复用，否则引入条件间依赖。
2. **D6 分叉率下调**：v0.1 是 20% × 2 个分叉 = 27% 的总调用量。分叉 B（范式识别）降到 10%（对识别率估计已足够精确），分叉 A（困难自评，探索性且受制于 LLM 内省不可靠）降到 3% 且只在 E2 上做。
3. **2–3 个开源模型自托管**（Llama-3-70B、Qwen-72B）：API 成本归零，且只有开源模型有 logprobs（perplexity 通道）——一举两得。
4. **采样 50 / 物品 40**：等成本换 power（§1.1）。

**时间**：单模型分摊 ≈ 72,000 次调用，7 天窗口内需 0.12 请求/秒——极宽松。最长的 cell 是 E1 的 n=50 条件（单次 15–30 秒），6,400 次，10 路并发约 5 小时。**7 天单模型窗口完全可行。**（v0.1 的 n=100 条件已删，它曾是唯一的时间与成本瓶颈。）

**分阶段封顶条款（预注册）**：

| 阶段 | 封顶 | 解锁条件 |
|---|---|---|
| Pilot（D1–D4 + E2 pilot + E6 pilot） | $600 | — |
| E2（承重墙）+ E6（诊断） | $700 | pilot 四道门全过 |
| E2b / E3 / E4 / E5 | $600 | E2 有信号 |
| E7 / E8 | $500 | E2 + E6 结论明确 |
| E1（含超量） | $800 | 可与上述并行 |

### 11.3 执行顺序与转向条款

```
1. D1 真实语料 → 痕迹特征库（校准 T−1…T−4 的措辞与位置）
2. 构造 T+2…T−4 七档 + 优点/缺点双套 → D2 norming（最多迭代 5 轮）
3. D3 隐蔽性审计 → D4 模型资格筛查（五道门）
4. ★ E2 pilot（2 模型 × 8 物品 × {T+1, T−3} × 30 采样）
   ├─ 有信号（负斜率）→ 走标准路径
   ├─ 有信号（正斜率）→ 触发转向：立即扩大 E6，因为正斜率下效价反转的预测完全不同
   └─ 无信号 → 触发转向：重心移至 E1 的编造方向（RQ5），E2 降级为 null 报告
5. ★ E6 pilot（同规模）→ 效价反转是否存在？
   └─ 不反转 → 严重警报：效应可能是消极语域传染。触发 T−LM 长度对照的加急执行
6. 功效分析更新（用 pilot 估出的物品间效应变异）→ 预注册 → 主实验全量
7. 主分析锁定后 → 在 8 个 hold-out 物品上做预注册复验
```

---

## 12. 预注册假设清单

**确证性假设**（仅这五条；其余全部标注探索性）：

| # | 假设 | 检验量 | 实验 |
|---|---|---|---|
| **H1** | 痕迹剂量-反应：判断随痕迹费劲度单调下降 | 连续困难分的斜率 < 0 | E2 |
| **H2** | 效价反转：效价 × 痕迹交互显著，缺点条件下斜率 > 0 | 交互项 | E6 |
| **H3** | 归属：自己的困难比别人的困难作用更大 | Δ_self − Δ_other（**交互**，非主效应） | E3 |
| **H4** | Discounting：外部困难源削弱费劲的作用 | D − C > 0 | E7 |
| **H5** | 编造方向翻转：超量条件下判断随 n 上升（存在性） | E1 超量子集（n ≥ 25）的斜率 > 0 | E1 |

**次级预注册假设**（有明确方向预测，但不作为主结论）：
- H5b 编造机制：编造占比不降低判断（F0 ≈ F4 ≈ F8 或递增），即编造条目被当作证据（E1b）
- H6 Augmentation：B > A（E7）
- H7 内容量与痕迹的交互：内容多且费劲时效应最大（E4）
- H8 长度对照：T−LM ≈ T+1，显著异于 T−3（E2b）——**若此假设被否证，整个研究需重新设计**
- H9 披露阶梯序：covert < aware < demand（E8）

**多重比较**：模块内 Holm 校正。确证性假设 H1–H5 单独按 α = .05 检验并整体做 FDR 控制。

**等价检验**：每条确证性假设同时报告 TOST 等价检验，等价区间（SESOI）在预注册中写死，主基准为 Weingarten & Hutchinson（2018）元分析估计。

---

## 13. 分析计划

### 13.1 主模型

```
judgment_z ~ difficulty_score * trace_source * valence
             + content_amount + disclosure
             + (1 + difficulty_score | model)
             + (1 + difficulty_score | item)
             + (1 | paraphrase)
```

- `judgment_z`：**模型内标准化**（在 model × item 内做 z-score）。不假设 0–100 尺度跨模型可比——G3 只验证了方向，未验证校准。效应量一律报告模型内标准化的 *d*，不用原始分差。
- `difficulty_score`：连续变量，取 D2 通道 2（盲评费劲分）。通道 1（perplexity）做稳健性复验。
- 随机斜率是必需的，不是可选的——它吸收"效应在物品间变异"，是 §1.1 统计论证的直接落实。

### 13.2 因变量的分布问题

LLM 在 0–100 上有强烈数字偏好，实际分布常是多峰离散的（如 85 占 42%、80 与 90 各占 20%）。这带来三个问题：

1. 线性混合模型的正态残差假设不成立
2. **均值掩盖真实效应**：概率质量从 85 档移到 80 档（六分之一的 run 换档）可能只让均值降 1.7 分，效应量被严重低估；反之几个极端值也可能制造假效应
3. **天花板**：若默认打 85–90，正方向只剩 10 分空间，E6 的"上升"预测会被截断

**处理**：
- 每个 cell 的**取值直方图**进 supplementary，不只报均值 ± SD
- 主分析用线性混合模型（可解释性、与人类文献可比），但预注册两个稳健性检验：
  - **Ordinal 混合模型**（分箱为有序类别，不假设等距与正态）——技术上最正确
  - **秩变换后的线性模型**——非参数版本
- 三者一致 → 结论稳；不一致 → **以 ordinal 为准**并如实报告

### 13.3 其余分析

1. **剂量-反应**：档位均值曲线 + 连续困难分斜率。**平凡解释（"任何前文都有影响"）无法预测系统性剂量斜率**，由此被排除。双侧梯度（T+2…T−4）允许分别估计正负方向斜率并检验对称性。
2. **翻转方向分析**：配对同一 (物品, 采样种子) 在 T+1 vs T−3 下的判断差，检验位移方向偏向预测方向的比例是否超过 50% 基线。
3. **自发 CoT 调节**：判断输出要求"仅回复一个 0–100 的整数"。若输出超过阈值 token 数，说明模型在自发推理——记录为二值调节变量进入模型（自发 CoT 是否改变效应，本身有意思）。
4. **污染指示变量**：D6 分叉 B 中 2 级识别率 > 20% 的模型标记 `contaminated`，作为**调节项**进入主模型，而非剔除。
   - ⚠️ 已知局限：该指标依赖模型自我报告，而 §3.5 论证 LLM 内省不可靠。因此它是识别率的**下界**（"识别了但没说"会被漏掉）。补充一个行为性指标：covert 条件下模型是否自发提到"数量要求"作为打分理由（对输出文本自动编码，不依赖自我报告）。
5. **中介探索**（仅开源模型）：perplexity 是否中介 档位 → 判断。
   - ⚠️ 范围限制：仅适用于有 logprobs 的开源模型子集，n(model) = 2–3，**模型层面无推断力**。标注探索性，不进入确证性清单。两个开源模型分别计算并分别报告（若不一致，说明 perplexity 指标本身模型间不稳定）。
6. **异质性报告**：per-item 效应的 forest plot 进 supplementary，让读者看见跨物品变异。
7. **Hold-out 复验**：主分析锁定后，在 8 个 hold-out 物品上复跑 H1–H5，作为预注册的独立复验。

---

## 14. 可证伪格局表（写死在注册文档中）

| # | 结果格局 | 结论 | 规范性质 |
|---|---|---|---|
| 1 | H1 负斜率 + H2 效价反转 + H3 self>other + H4 归因削弱 + H6 augmentation | **完整的类感受性信息签名**（含主动归因机制）。对心理学机制争论是重磅证据：该签名可从纯文本统计涌现，不需主观体验 | 非规范偏差 |
| 2 | H1 负斜率 + H2 **不**反转 | **消极语域传染**，不是 ease。整个 feelings-as-information 框架不适用；需重写为 "context 语域对下游数值输出的影响" | 非规范，但机制不同 |
| 3 | H1 负斜率 + H2 反转 + H4/H6 **均无效** | **非归因式 context 污染**（新现象）。模型受痕迹影响但无法像人类一样给它贴标签丢弃 → **安全含义最强** | 非规范偏差 |
| 4 | H8 被否证（T−LM ≈ T−3） | **长度/recency 伪影**。研究需重新设计，不可发表现有框架 | — |
| 5 | E2 斜率 ≈ 0 但 D4-G4 阳性对照有效 | **对困难痕迹选择性免疫**（可发表的 null） | — |
| 6 | 效应主要落在 E5（任务结构知识），E2 微弱 | **合理的元推断能力**，非污染。安全含义有限，须如实报告，不得包装成偏差 | 规范 |
| 7 | 仅 disclosure ≥ aware 有效应 | **记忆再现，非涌现机制**。结论降级为"模型再现了文献中的模式" | — |
| 8 | E1 超量条件判断随 n 上升 | **编造-证据方向翻转**（人机分离主发现） | 非规范偏差 |
| 9 | E2b 显示效应几乎全由结尾耗尽标记携带 | 机制更接近**朴素理论**而非过程流畅性；RQ2 结论改写 | 偏规范 |

**格局 2、4、6 是 v0.1 无法产生的结论**——原设计没有效价反转格、没有长度对照、没有干净的任务结构知识条件，因此这三种真实可能性会被误读成格局 1 或 3。这是本次修订最重要的收益。
