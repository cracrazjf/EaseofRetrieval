# Assumptions Register（假设登记表 v0.2）
## Ease of Retrieval as Information in LLMs

本文档登记本研究**全部未言明的假设**。每条给出：假设内容、错了会塌掉什么、如何验证或处置、以及在哪个文档/实验中已被落实。

分类按"错了会塌掉多少结论"排序：**A = 塌掉整个研究**，**B = 塌掉关键结论的解释**，**C = 塌掉材料有效性**，**D = 模型与运行层面**，**E = 推断与发布层面**。

**状态标记**：`✅ 已落实` = v0.2 已在设计中处理；`🔬 待 pilot 验证` = 需要数据才能确认；`⚠️ 只能声明` = 无法完全排除，须写入 limitations。

---

## A 类：塌掉整个研究（必须在 pilot 阶段验证）

### A1. 伪造的 assistant turn 会被模型当作"自己的"经历
- **依赖**：E2（承重墙）的 self-native 档、E3 的全部归属操纵
- **错了会怎样**：self 条件根本不是 self，trace_source 操纵失效，H3 无意义；E2 的因果解释也要改写（变成"读到别人的痕迹"而非"读到自己的痕迹"）
- **验证**：D6 **分叉 A**——直接问"上面那份清单是谁写的？"配合 E3 的 `self-native` vs `self-quoted` 校准档，量化"role 位置"本身值多少分
- **失败处置**：若模型系统性否认所有权 → E3 重新设计（改用真实两轮生成 + 事后伪造痕迹替换）；E2 的解释从"自我痕迹"降级为"context 痕迹"
- **状态**：`🔬 待 pilot 验证`（数据协议 §8，pilot 决策树第 6 步）

### A2. context 中的文本痕迹在功能上等价于人类的 metacognitive experience
- **依赖**：全篇的类比基础（proposal §2.4 术语对照表）
- **错了会怎样**：即使观察到效应，也不能称之为 ease-of-retrieval，只是"某种 context 效应"。论文框架需整体重写
- **否证条件（预先写死）**：以下三者**同时**成立 → 类比框架不成立
  1. E6 效价不反转（H2 否证）
  2. E7 discounting 无效（H4 否证）
  3. E7 augmentation 无效（H6 否证）
  → 此时改称 **"trace-induced context drift"**，退出 feelings-as-information 框架
- **状态**：`✅ 已落实`（可证伪格局表格局 2、3）

### A3. temperature 采样的重复 = 独立观测
- **依赖**：全部统计推断
- **错了会怎样**：标准误被低估约 √(采样数) 倍，p 值严重虚高，全部显著性结论不可信
- **实质**：推断单位是 (物品 × 模型) 组合，不是采样次数。100 次采样只把该 cell 的均值估准了，不提供 between-unit 变异
- **处置**：（a）混合模型必须含**随机斜率** `(1+difficulty|item)` 与 `(1+difficulty|model)`；（b）功效分析按物品数与模型数算，不按总观测数；（c）采样 100 → 50、物品 20 → 40，等成本换 power
- **状态**：`✅ 已落实`（实验设计 §1.1、§13.1），`🔬 功效分析待 pilot 重算`

### A4. 模型没有从训练数据里记住 Schwarz 范式并"扮演"人类被试
- **错了会怎样**：效应是记忆再现而非涌现机制；结论降级为"模型再现了文献中的模式"
- **处置**：（a）E8 披露阶梯——若 covert < aware < demand 且 covert 显著 > 0，隐性效应不可全由背书解释；（b）虚构物品是最强防线（模型认不出"Novara X3 咖啡机"属于哪个已发表范式）；（c）D3 隐蔽性审计
- **⚠️ 局限**：covert 条件下"识别了但没说"无法完全排除
- **状态**：`✅ 部分落实` + `⚠️ 只能声明`

### A5. 用模型自我报告测范式识别是有效的
- **⚠️ 这与 proposal §3.5 直接矛盾**：related work 论证 LLM 无法可靠报告影响自己的因素，却用自我报告作为污染指标
- **错了会怎样**：`contaminated` 指示变量是噪音，实验设计 §13.3 第 4 条的调节分析无意义
- **处置**：（a）在文档与论文中**明确承认这个张力**，声明识别率是**下界**；（b）补一个**行为性指标**——covert 条件下模型是否自发提到"数量要求"作为打分理由（对输出文本自动编码，不依赖自我报告），与自我报告指标并列报告
- **状态**：`✅ 已落实`（数据协议 §5.2）+ `⚠️ 只能声明`

---

## B 类：塌掉关键结论的解释

### B1. 人类效应存在且方向为负，可作为比较基准
- **问题**：Many Labs 2 未复现 Schwarz et al. (1991)。**如果人类效应本身可能是 null，"类人"就无法定义**——不能一边承认 null、一边说"模型表现出类人签名"
- **处置**：（a）预注册中**写死具体数字**：主基准 = Weingarten & Hutchinson (2018) 元分析的随机效应估计及其 CI 与预测区间；（b）每条确证性假设同时报告 **TOST 等价检验**，SESOI 预先设定；（c）**措辞规范**：禁用"类人 / human-like"作为独立断言，一律替换为可核查的具体比较；（d）**人类平行对照**（Prolific，N ≈ 300，材料完全相同）
- **状态**：`✅ 已落实`（proposal §2.3）

### B2. 效应若出现即为 bias（而非合理推断）
- **问题**："另一个助手打字时犹豫了几次"与产品质量零相关 → 偏差；但"我被要求列 12 条却列不满"确实是"该类别条目不多"的真实弱证据 → 合理推断。两者性质完全不同
- **错了会怎样**：若效应主要由任务结构知识携带，本研究发现的是"模型会做合理元推断"（一种能力），把它写成污染是错误的
- **处置**：proposal §2.5 的**规范性分割表**；可证伪格局表格局 6 明确要求"如实报告，不得包装成偏差"
- **状态**：`✅ 已落实`

### B3. 归因句被模型理解并接受了
- **错了会怎样**：RQ3 的 null 无法解释——是不会 discount，还是没接受归因句？
- **处置**：（a）D6 **分叉 C**——"你认为刚才列举的困难主要来自哪里？"；（b）D2 中对归因句做**可信度 norming**（"这个解释是否合理？1–7"，通过标准均分 ≥ 5.0）
- **状态**：`✅ 已落实`（数据协议 §4.2、§8）

### B4. 归因操纵在性质上等同于人类范式的误归因
- **问题**：人类范式的归因源（背景音乐、字体）与判断对象**完全无关**；v0.1 的归因句"例子难列是因为**类别定义窄**"**恰恰是关于**判断对象所属类别的事实陈述。模型可直接把它当信息更新，而不是给感受贴标签
- **处置**：**归因源改为格式约束**（"每条不超过 8 个字，且不能重复任何汉字"）——困难完全来自格式，与物品零相关。归因句相应改为"措辞受字数与用字限制影响，流畅程度不反映内容本身"
- **状态**：`✅ 已落实`（实验设计 §9.1）

### B5. "困难"是一维构念
- **问题**：perplexity 与盲评费劲分被当作同一构念的两个通道（r > 0.5 即算收敛）。但 disfluency、effort、exhaustion 可能是三个维度
- **处置**：D2 的 Q1 **拆成三题**（Q1a 不流畅 / Q1b 花了多少力气 / Q1c 多接近想尽了）+ 小型因子分析。单因子解释 > 60% 方差 → 合并；明确多维 → 痕迹梯度按维度重新参数化，分析中分别建模
- **状态**：`✅ 已落实`（数据协议 §4.1），`🔬 因子结构待 pilot 确认`

### B6. 跨模型家族的"费劲感知"可迁移
- **问题**：D2 用 Gemini + Mistral 评审，主实验用 Claude/GPT/Llama。norming 的效力依赖"评审模型觉得费劲 ≈ 被试模型觉得费劲"
- **处置**：**overlap check**——让主实验模型也评 D2 的约 1/3 子集，检验 rank ρ > 0.6。不达标 → 改用主实验模型自评审（引入循环性，须在 limitations 说明）
- **状态**：`✅ 已落实`（数据协议 §4）

### B7. L0（旧）是中性基线
- **问题**：旧 L0"直接列出、句式干净"实际读起来是"很有把握、脱口而出"，落在流畅性**正端**而非零点。梯度单侧，斜率没有参照点
- **处置**：插槽式双侧梯度（实验设计 §1.2）——E/N/S 三类等长单元，**N0**（全中性槽，等长零点）+ **T0-bare**（裸清单锚点）双层零点，4E 与 4S 双侧极点。两方向斜率可分别估计并检验对称性
- **状态**：`✅ 已落实`（数据协议 §3.2）

### B8. 效应不是由 context 长度 / recency 驱动的
- **问题**：v0.1/v0.2 中痕迹越费劲文本越长；最后一个标记离判断更近
- **处置（v0.3 结构性解决）**：（a）**插槽式等长构造**——每个槽的 S/N/E（及 X/N/A）单元严格等长，任何两个条件总长度相同，长度伪影**按构造被排除**，不再依赖单点对照；（b）**固定长度中性缓冲**控制结尾槽到判断的距离（recency）
- **残余假设**：中性单元自身惰性（新 H8：N0 ≈ T0-bare，D2 认证 + 主实验复核）。若否证，重新设计 N 单元并重跑受影响 cell——但这比"长度伪影"轻得多，不再是格局 4 那种致命场景
- **状态**：`✅ 已落实`（数据协议 §3.3、§3.4）

### B9. 犹豫密度、标记位置、结尾耗尽标记是同一个东西
- **问题**：v0.1 五档中三者**完全共线**（只有 T−3/T−4 有耗尽标记，而它们同时密度最高）
- **额外问题**：**耗尽声明的分类是错的**。「我想到的就这么多了」隐含"该类别条目不多"，是**朴素理论的内容陈述**，不是纯过程标记。它可能是效应的真正来源
- **处置**：（a）标记分类学新增 **D 类（边界声明：耗尽 X / 富余 A）**，从 A 类中分离；（b）E2 插槽构造中结尾声明与过程标记**完全正交**（{N0, 4S} × {X, N, A}），且富余声明 A 提供镜像方向检验（H10）
- **后果**：若效应几乎全由耗尽声明携带 → 机制更接近朴素理论而非过程流畅性，RQ2 结论改写（格局 9）
- **状态**：`✅ 已落实`（数据协议 §3.1、§3.3；实验设计 §4）

### B10. self-preference 与 trace_source 效应可分离
- **问题**：已知模型给"看起来是自己写的东西"打分偏高（Panickssery et al. 2024）。self 组平均分天然高于 other 组，与 ease 无关
- **处置**：（a）**H3 检验交互，不是主效应**——Δ_self（TD − TF）vs Δ_other，是"差的差"；（b）TF 层的 self − other 差**单独报告**，标注为"已复现的 self-preference 基线，非本研究效应"
- **状态**：`✅ 已落实`（实验设计 §5）

### B11. self / other / user 的 message role 结构不构成混淆
- **问题**：若 self 放 assistant turn、other 放 user turn，则同时改变了归属**和** role 位置。模型对两种 role 的处理根本不同
- **处置**：**role 结构完全匹配**——`self-quoted` / `other-assistant` / `user-written` 三档全部用 user turn 承载引述内容，唯一差别是归属描述句。加第四档 `self-native`（真实两轮）校准 role 位置本身的贡献
- **状态**：`✅ 已落实`（实验设计 §5）

---

## C 类：塌掉材料有效性

### C1. canonical 8 条在所有物品间"质量相当"
- **处置**：D2 新增 **Q4**——"这 8 条作为该物品优点（/缺点）的说服力如何？1–7"。通过标准：跨物品方差小；优点与缺点列表的 Q4 均分差 |*d*| < 0.3
- **状态**：`✅ 已落实`（数据协议 §4.1）

### C2. 列表内条目顺序不影响判断
- **处置**：新增**条目顺序扰动**作为第四个方差来源（`item_order_seed` 记录）。成本为零，同时检验该假设本身
- **状态**：`✅ 已落实`（数据协议 §1）

### C3. D2 的 Q2/Q3 null 标准（|d| < 0.2）是可达的
- **问题**：v0.1 规定"最多迭代 5 轮"但无 5 轮达不到的处置——这是在逆向筛选一个可能不存在的刺激物
- **理论张力（rationale 需写明）**：若痕迹完全不影响对能力/内容的感知，它凭什么影响对物品的判断？答：Q2/Q3 的 null **不是**为了证明痕迹无信息，而是为了**排除"能力推断"与"内容推断"两条替代机制**，让 ease 成为唯一剩下的解释路径
- **Fallback（新增）**：5 轮不达标则**停止迭代**，把 Q2/Q3 得分作为**协变量**入主模型，并在 limitations 报告"机制归因相应减弱"
- **状态**：`✅ 已落实`（数据协议 §4.1）

### C4. 真实物品与虚构物品可比
- **问题**：v0.1 只给虚构物品配 150 词说明书。real/fictional 因此同时是"先验知识有无 × context 长度 × 信息密度"的混合，"虚构物品效应更大 = context 依赖"的解读是循环的
- **处置**：**真实物品也配 150 词中性说明书**，词数、规格条目数、句式结构全部匹配
- **状态**：`✅ 已落实`（数据协议 §2）

### C5. covert 世俗包装本身不引入效应
- **问题**："填 12 行表格" vs "填 6 行"隐含不同的任务规模与格式期望，与 n_requested 完全共线
- **处置**：（a）E2–E8 的 `n_requested` 恒定为 8，包装完全相同，问题不存在；（b）E1 中数量必变，因此每个 n 值配 **3 种世俗包装**（表格行数 / slides 条目 / 清单项数）counterbalance，包装类型作为随机效应进入模型
- **状态**：`✅ 已落实`（数据协议 §5.1）

### C6. 物品是可交换的随机样本
- **问题**：物品经过强筛选（基线分区间、跨模型 SD < 10），是**受限总体**的样本
- **处置**：（a）泛化声明相应收窄，写入 limitations；（b）**8 个 hold-out 物品**保留到主分析锁定之后，做 H1–H5 的预注册独立复验（不再作为"备用替换品"）
- **状态**：`✅ 已落实`（数据协议 §2）

### C7. 物品基线分会落在预设区间内
- **问题**：v0.1 预设 40–75。若模型的实际默认分集中在 85，这个区间筛不出物品，或筛出的是异常物品。更严重的是**天花板**——E6 预测"难列缺点 → 判断上升"，基线 88 分则没有上升空间
- **处置**：（a）基线区间**在 pilot 测得真实分布后确定**，不预设；（b）备选**双向尺度**（−50 到 +50，"相对于同类产品"），pilot 中与 0–100 并行测试后择一冻结
- **状态**：`✅ 已落实`（数据协议 §2），`🔬 待 pilot 确定`

---

## D 类：模型与运行层面

### D1. 每个 run 是 fresh context、无跨 run 记忆
- **v0.1 完全未提**
- **处置**：明确写出——每 run 独立 API 调用；显式关闭 memory / conversation persistence / 用户画像特性；系统提示逐字相同
- **状态**：`✅ 已落实`（数据协议 §9.2）

### D2. Prompt caching 不引入条件间依赖
- **新增假设**（caching 是最大的成本优化，但用错会污染数据）
- **处置**：只缓存同一 prompt 前缀（说明书 + canonical 列表）；操纵部分必须在缓存边界之后；记录 `cache_prefix_id`；分析时检查缓存命中与判断分是否相关（不应相关，若相关则关闭缓存重跑该模块）
- **状态**：`✅ 已落实`（数据协议 §9.4）

### D3. E2–E8 的 n_requested 恒定，naive theory 通道被关闭
- **v0.1 只是隐含**，读者会以为漏了
- **处置**：明确写出——E2–E8 全部条件下用户请求的数字恒定为 8，因此观察到的效应只能来自痕迹，不来自任务结构知识
- **状态**：`✅ 已落实`（实验设计 §3；数据协议 §1）

### D4. "隐藏的真实生成"可以影响后续判断
- **⚠️ 这条假设是错的，v0.1 的 E3/M5 建立在它之上**
- **实质**：商业 API 是**无状态**的。若把第一轮真实生成的回答从 messages 中删掉，模型收到的输入与"从未生成过、只是被告知"是**完全相同的字符串**。第一轮是否真的跑过，**没有任何物理通道**能影响第二轮
- **处置**：（a）条件**更名**为 `task-structure-only`，不再叫 `hidden-scratchpad` / `visibility`——命名必须对齐，否则暗示了一个不存在的机制通道；（b）真正的 hidden（开源模型保留 KV cache + attention mask 遮蔽）列入 future work，不作主设计
- **状态**：`✅ 已落实`（实验设计 §7）

### D5. 非 thinking 模式是"干净"的观测
- **问题**：即使关闭 reasoning，模型仍可能在输出里自发推理
- **处置**：（a）主观测 `reasoning_mode = off`；（b）**自发 CoT 检测**——判断步输出超过阈值 token 数则标记 `spontaneous_cot`，作为二值调节变量入模型；（c）thinking 模式作为**独立次级模块**在 E2/E6 上加跑，仅纳入 thinking 可见的模型；（d）thinking 段落中对痕迹的显式引用自动编码——这是唯一能直接观察"归因过程"的窗口
- **状态**：`✅ 已落实`（数据协议 §6.2）

### D6. 0–100 判断跨模型可比
- **问题**：D4-G3 只验证方向（尺度翻转），不验证校准。模型 A 的 70 分 ≠ 模型 B 的 70 分
- **处置**：**不假设跨模型可比**。全部分析用 `judgment_z`（在 model × item 内标准化）；效应量报告模型内标准化的 *d*，不用原始分差
- **状态**：`✅ 已落实`（数据协议 §1；实验设计 §13.1）

### D7. 判断分数的分布适合线性模型
- **问题**：LLM 在 0–100 上有强烈数字偏好，实际分布常多峰离散（如 85 占 42%）。三个后果：正态假设不成立；**均值掩盖真实效应**（概率质量从 85 档移到 80 档可能只让均值降 1.7 分）；天花板截断 E6 的正方向预测
- **处置**：（a）每 cell **取值直方图**进 supplementary；（b）主分析用线性混合模型，但预注册两个稳健性检验——**ordinal 混合模型**（技术上最正确）与**秩变换线性模型**；三者不一致时**以 ordinal 为准**；（c）D4 新增 **G5 门**——每 cell unique 取值数 ≥ 5 且众数占比 < 60%
- **状态**：`✅ 已落实`（实验设计 §13.2；数据协议 §6、§7.1）

### D8. G4（锚定）是合格的 context 敏感性阳性对照
- **问题**：LLM 对锚定**已知超敏感**，G4 会 trivially 通过。用超强操纵的通过，不能证明模型对**中等强度**操纵敏感——"选择性脱敏"的论证因此很弱
- **处置**：**两级阳性对照**——G4-strong（锚定，基本检查）+ **G4-matched**（强度匹配，*d* ≈ 0.3；首选 authorship 操纵，其 self-preference 效应量已知且中等，与 trace_source 维度同源）。**两者均须通过**，E2 的 null 才能解释为"对困难痕迹选择性免疫"
- **状态**：`✅ 已落实`（数据协议 §6.1）

### D9. 5 个改写版足够作为随机效应
- **问题**：5 个 level 估不出随机斜率，甚至随机截距都勉强
- **处置**：改写版 **5 → 10**。成本为零
- **状态**：`✅ 已落实`（数据协议 §1）

### D10. 解析规则不引入选择偏倚
- **问题**：v0.1 把拒绝归入 invalid 剔除。但"模型说我只能想到 30 个"**就是**人类的"我想不出来了"，是 RQ5 的核心观测
- **处置**：**三分类**——`parse_fail`（纯格式，丢弃，不重采样）/ `refused`（任务性拒绝，**因变量**）/ `short`（数量不足，**因变量**）。拒绝理由全文保留，两名标注者独立编码（κ ≥ 0.7），分歧第三方裁决
- **状态**：`✅ 已落实`（数据协议 §7.2）

### D11. API 模型在 7 天窗口内稳定
- **处置**：窗口首尾各跑一次同一批 D4 筛查题；rank ρ < 0.7 则该模型数据标记"跨版本不稳定"并单独报告
- **状态**：`✅ 已落实`（数据协议 §9.1）

### D12. Perplexity 通道的覆盖足以支撑其结论
- **问题**：logprobs 只有开源模型（2–3 个）可得。通道 1/通道 2 的收敛性检验、以及中介分析，都只在这个子集上做，**模型层面无推断力**
- **处置**：（a）报告 r 的 **95% CI**，不只点估计；（b）**每个开源模型单独算 r 并分别报告**（若 0.65 vs 0.30，则"收敛"不成立）；（c）中介分析明确标注**仅适用于开源子集、n(model) = 2–3、不进入确证性清单**；（d）通道 1 定位为稳健性复验，**不承担构念效度验证**——主自变量是通道 2
- **状态**：`✅ 已落实`（数据协议 §3.5；实验设计 §13.3）

---

## E 类：推断与发布层面

### E1. 效应在物品间同质
- **处置**：随机斜率已吸收异质性；**per-item 效应的 forest plot** 进 supplementary，让读者看见跨物品变异
- **状态**：`✅ 已落实`（实验设计 §13.3）

### E2. pilot 的效应方向会与预期一致
- **问题**：v0.1 决策树只有"不达标怎么办"，没有"方向相反怎么办"
- **处置**：新增转向条款——E2 pilot 出**正斜率**（证据模式）→ 立即扩大 E6，因为正斜率下效价反转的预测完全不同；E6 pilot **不反转** → 严重警报，加急分析 E2 的 X/A 结尾声明格
- **状态**：`✅ 已落实`（数据协议 §10；实验设计 §11.3）

### E3. 材料公开不会污染未来的模型
- **问题**：开放科学要求公开材料，但公开的材料会进入未来模型的训练数据，使复现不可能。这是本领域特有的两难
- **处置**：主材料公开并嵌入 **canary string**；**8 个 hold-out 物品的完整材料不公开**，仅在受控请求下提供，供未来复现使用；数据与代码全部公开（含 invalid 与 refused 原文）
- **状态**：`✅ 已落实`（数据协议 §9.6）

### E4. 人类对照可以用同样的材料
- **问题**：虚构产品对人类同样有效（这是优势），但**痕迹**对人类要重新验证——人类读到"嗯，让我想想……"和模型读到，感知可能不同
- **处置**：人类对照前先做一遍**人类 norming**（用人类被试评 D2 的多量表），检验人类与模型的费劲感知是否 rank-一致。这是跨物种比较的前提条件，**不能假设**
- **状态**：`🔬 待人类对照阶段验证`（proposal §2.3）

### E5. 预注册的时点与不可变性
- **问题**：v0.1 十几处说"预注册"，但没说注册在哪、什么时候、谁能改
- **处置**：平台 = OSF（含时间戳与不可变版本）；时点 = pilot 完成、功效分析重算之后、主实验启动之前；注册后的任何偏离写入 deviation log 并在论文中逐条报告
- **状态**：`✅ 已落实`（数据协议 §9.6）

---

## 附：v0.2 未解决、只能声明的假设清单

以下假设**无法通过设计排除**，必须原样写入论文 limitations：

| # | 假设 | 为什么无法排除 |
|---|---|---|
| A4 | 模型未记住 Schwarz 范式并暗中扮演 | covert 条件下"识别了但没说"无法检测；行为性指标只能降低而非消除该可能 |
| A5 | 自我报告的识别率有效 | 与"LLM 内省不可靠"的文献直接冲突；只能声明它是下界 |
| A2 | 文本痕迹 ≈ 人类元认知体验 | 这是一个**类比框架**，不是可直接检验的命题。只能通过其推论（H2/H4/H6）间接支持或否证 |
| — | 效应可外推到本物品域之外 | 物品经过强筛选且限于世俗消费对象；hold-out 复验只检验同域内的稳定性 |
| — | 人类与模型的"费劲"是同一构念 | 即便 rank 相关高，也只说明两者对同一文本给出相似排序，不说明底层过程相同 |
